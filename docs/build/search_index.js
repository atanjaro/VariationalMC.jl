var documenterSearchIndex = {"docs":
[{"category":"section","location":"examples/hubbard_ladder_mpi.html#Hubbard-Ladder-with-MPI-Parallelization","page":"Hubbard Ladder with MPI Parallelization","text":"","title":"Hubbard Ladder with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladder_mpi.html","page":"Hubbard Ladder with MPI Parallelization","text":"Download this example as a Julia script.","title":"Hubbard Ladder with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladder_mpi.html","page":"Hubbard Ladder with MPI Parallelization","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a rectangular lattice goemetry (ladder) with a stripe wavefunction using MPI parallelization.","title":"Hubbard Ladder with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladder_mpi.html","page":"Hubbard Ladder with MPI Parallelization","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\nusing MPI\n\n# Top-level function to run the simulation\nfunction run_simulation(\n    comm::MPI.Comm;         # MPI communicator.\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    Lx,                     # System size in the x-direction.\n    Ly,                     # System size in the y-direction.\n    t′,                     # Next-nearest neighbor hopping amplitude.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_tp%.2f_U%.2f_Lx%d_Ly%d\" density t′ U Lx Ly\n\n    # Get MPI process ID.\n    pID = MPI.Comm_rank(comm)\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID,\n        pID = pID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [Lx, Ly],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Define the positive next nearest-neighbor bond.\n    bond_xy = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,1]\n    )\n    bond_xy_id = add_bond!(model_geometry, bond_xy)\n\n    # Define the negative next nearest-neighbor bond.\n    bond_yx = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,-1]\n    )\n    bond_yx_id = add_bond!(model_geometry, bond_yx)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                                      # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y, bond_xy, bond_yx],       # defines hopping.\n        t_mean  = [t, t, t′, t′]                             # defines corresponding mean hopping amplitude.\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )\n\n    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add charge stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"density\",\n        order_type  = \"charge\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1],\n        p           = [0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.3, 0.3, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.3, 0.3],\n        model_geometry = model_geometry\n    )\n\n    # Add spin stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"spin-z\",\n        order_type  = \"spin\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1],\n        p           = [0.5, -0.5, 0.5, -0.5, 0.5, -0.5, 0.0, 0.0, -0.5, 0.5, -0.5, 0.5, -0.5, 0.5, 0.0, 0.0],\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"spin-spin\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize site-dependent density measurements.\n    initialize_site_dependent_measurements!(\n        measurement_container   = measurement_container,\n        model_geometry          = model_geometry,\n        observable              = \"density\"\n    )\n\n    # Initialize site-dependent spin measurements.\n    initialize_site_dependent_measurements!(\n        measurement_container   = measurement_container,\n        model_geometry          = model_geometry,\n        observable              = \"spin-z\"\n    )\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Initialize MPI\n    MPI.Init()\n\n    # Initialize the MPI communicator.\n    comm = MPI.COMM_WORLD\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(\n        comm;\n        sID             = parse(Int,     ARGS[1]),\n        L               = parse(Int,     ARGS[2]),\n        t′              = parse(Float64, ARGS[3]),\n        U               = parse(Float64, ARGS[4]),\n        density         = parse(Float64, ARGS[5]),\n        ph_transform    = parse(Bool,    ARGS[6]),\n        N_equil         = parse(Int,     ARGS[7]),\n        N_opt           = parse(Int,     ARGS[8]),\n        N_sim           = parse(Int,     ARGS[9]),\n        N_opt_bins      = parse(Int,     ARGS[10]),\n        N_sim_bins      = parse(Int,     ARGS[11])\n    )\n\n    # Finalize MPI.\n    MPI.Finalize()\nend","title":"Hubbard Ladder with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_ladders.html#3)-Interacting-Hubbard-Ladders-with-a-Stripe-Wavefunction","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Download this example as a Julia script.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In this tutorial, we will work through a more involved example of simulating a Hubbard model on N-leg interacting ladders using a variational wavefunction with charge and spin stripes. The Hubbard Hamiltonian for N-leg ladders (rectangular lattices) is given by","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"beginalign\nhatH =  -t sum_langle i j rangle alpha sigma (hatc^dagger_i alpha sigma hatc^phantom dagger_j alpha sigma + rm Hc)\n + U sum_ialpha hatn_ialphauparrowhatn_ialphadownarrow\n + V sum_isigmasigma left( n_iN2sigma n_iN2+1sigma + n_iN2sigma n_i+1N2+1sigma right)\n - mu sum_isigma hatn_isigma\nendalign","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"where hatc^dagger_ialphasigma  (hatc^phantom dagger_ialphasigma) creates (annihilates) a spin sigma electron on site i in the alpha sublattice, and hatn_ialphasigma = hatc^dagger_ialphasigma hatc^phantom dagger_ialphasigma is the spin-sigma electron number operator for site i in sublattice alpha. In the above Hamiltonian U  0 controls the strength of the on-site Hubbard repulsion and V0 controls the strength of the interaction between the N-leg ladders.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_import_packages","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Import packages"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Let us begin by importing VariationalMC.jl, and its relevant submodules.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"using VariationalMC\nimport VariationalMC.LatticeUtilities as lu","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"The VariationalMC.jl package re-exports the LatticeUtilities from SmoQyDQMC, which we will use to define the lattice geometry for our model.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"We will also  use the Standard Library packages Random and Printf for random number generation and C-style string formatting, respectively.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"using Random\nusing Printf","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Specify-simulation-parameters","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Specify simulation parameters"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"The entire main body of the simulation we will wrapped in a top-level function named run_simulation that will take as keyword arguments various model and simulation parameters that we may want to change. The function arguments with default values are ones that are typically left unchanged between simulations. The specific meaning of each argument will be discussed in more detail in later sections of the tutorial.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    Lx,                     # System size in the x-direction.\n    Ly,                     # System size in the y-direction.\n    t₁′,                    # Next-nearest neighbor hopping amplitude in the first ladder.\n    t₂′,                    # Next-nearest neighbor hopping amplitude in the second ladder.\n    U,                      # Hubbard interaction.\n    V,                      # Extended Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_initialize_simulation","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Initialize simulation"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In this first part of the script we name and initialize our simulation, creating the data folder our simulation results will be written to. This is done by initializing an instances of the SimulationInfo type, and then calling the initialize_datafolder function.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Note that the write_bins_concurrent keyword arguments controls whether or not binned simulation measurement data is written to HDF5 file during the simulation, or held in memory and only written to file once the simulation is complete. Here we decide how to set write_bins_concurrent based on the system size being simulated. This is because when performing simulations of small systems that do not take very long, writing data to file too frequently can sometimes cause network latency problems on clusters and HPC systems. However, for larger systems that take longer to simulate, you are not limited by file IO frequency but rather by available memory, so writing data to file more frequently is preferred in these cases.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_ladder_n%.3f_tp1%.2f_tp2%.2f_U%.2f_V%.2f_Lx%d_Ly%d\" density t₁′ t₂′ U V Lx Ly\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Initialize-simulation-metadata","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Initialize simulation metadata"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In this section of the code we record important metadata about the simulation, including initializing the random number generator that will be used throughout the simulation. The important metadata within the simulation will be recorded in the metadata dictionary.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In the above, sID stands for simulation ID, which is used to distinguish simulations that would otherwise be identical i.e. to distinguish simulations that use the same parameters and are only different in the random seed used to initialize the simulation. A valid sID is any positive integer greater than zero, and is used when naming the data folder the simulation results will be written to. Specifically, the actual data folder created above will be \"$(filepath)/$(datafolder_prefix)-$(sID)\". Note that if you set sID = 0, then it will instead be assigned smallest previously unused integer value. For instance, suppose the directory \"$(filepath)/$(datafolder_prefix)-1\" already exits. Then if you pass sID = 0 to SimulationInfo, then the simulation ID sID = 2 will be used instead, and a directory \"$(filepath)/$(datafolder_prefix)-2\" will be created.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Another useful resource in the documentation is the Simulation Output Overview page, which describes the output written to the data folder generated during a VariationalMC.jl simulation.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Initialize-model","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Initialize model"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"The next step is define the model we wish to simulate. In this example the relevant model parameters are the Hubbard interaction strength U (U) and lattice size L (L).","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"First we define the lattice geometry for our model, relying on the LatticeUtilities package to do so. We define a the unit cell and size of our finite lattice using the UnitCell and Lattice types, respectively. Lastly, we define various instances of the Bond type to represent the the nearest-neighbor and next-nearest-neighbor bonds. All of this information regarding the lattice geometry is then stored in an instance of the ModelGeometry type. Further documentation, with usage examples, for LatticeUtilities package can be found here.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"We also initialize the Canonical Ensemble particle configuration that lives on the lattice. In this case, given the total Fermion density that we want to live on the lattice, ParticleConfiguration will determine the correct total number of particles and the correct number of spin-up and spin-down particles.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0], [1.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions in the y-direction.\n    lattice = lu.Lattice(\n        [Lx, Ly],\n        [false, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor x-bond in the first ladder.\n    bond_x1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x1_id = add_bond!(model_geometry, bond_x1)\n\n    # Define the nearest-neighbor y-bond in the first ladder.\n    bond_y1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y1_id = add_bond!(model_geometry, bond_y1)\n\n    # Define the postive next-nearest neighbor in the first ladder.\n    bond_xy1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_xy1_id = add_bond!(model_geometry, bond_xy1)\n\n    # Define the negative next-nearest neighbor in the first ladder.\n    bond_yx1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,-1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_yx1_id = add_bond!(model_geometry, bond_yx1)\n\n    # Define the nearest-neighbor x-bond in the second ladder.\n    bond_x2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x2_id = add_bond!(model_geometry, bond_x2)\n\n    # Define the nearest-neighbor y-bond in the second ladder.\n    bond_y2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y2_id = add_bond!(model_geometry, bond_y2)\n\n    # Define the postive next-nearest neighbor in the second ladder.\n    bond_xy2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_xy2_id = add_bond!(model_geometry, bond_xy2)\n\n    # Define the negative next-nearest neighbor in the second ladder.\n    bond_yx2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,-1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_yx2_id = add_bond!(model_geometry, bond_yx2)\n\n    # Define a nearest-neighbor interaction bond between the first and second ladders.\n    bond_int1 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [-1,-1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_int1_id = add_bond!(model_geometry, bond_int1)\n\n    #$ Define a nearest-neighbor interaction bond between the first and second ladders.\n    bond_int2 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [0,-1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_int2_id = add_bond!(model_geometry, bond_int2)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Next we specify the non-interacting tight-binding term in our Hamiltonian with the TightBindingModel type.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Set nearest-neighbor hopping amplitude for the first ladder to unity.\n    t₁ = 1.0\n\n    # Set nearest-neighbor hopping amplitude for the second ladder to unity.\n    t₂ = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0., # set initial estimate for the chemical potential\n        t_bonds = [bond_x1, bond_y1, bond_x2, bond_y2, bond_xy1, bond_yx1, bond_xy2, bond_yx2], # defines hopping\n        t_mean  = [t₁, t₁, t₂, t₂, t₁′, t₁′, t₂′, t₂′] # defines corresponding mean hopping amplitude\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Then, we define the Hubbard interaction with the HubbardModel type and the extended Hubbard interaction with the ExtendedHubbardModel.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1, 2],\n        U_mean      = [U, U]\n    )\n\n    # Define the Hubbard interaction between the ladders.\n    extended_hubbard_model = ExtendedHubbardModel(\n        model_geometry = model_geometry,\n        V_bond = [bond_int1, bond_int2],\n        V_mean = [V, V]\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Initialize-model-parameters","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Initialize model parameters"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"The next step is to initialize our model parameters given the size of our finite lattice. To clarify, both the TightBindingModel and HubbardModel types are agnostic to the size of the lattice being simulated, defining the model in a translationally invariant way. To do so we need to initialize an instance of the TightBindingParameters and HubbardParameters types.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Note that the initial guess for the chemical potential given in TightBindingModel will be overriden here with the exact one.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    #$ Initialize tight-binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard interaction parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize extended Hubbard interaction parameters.\n    extended_hubbard_parameters = ExtendedHubbardParameters(\n        extended_hubbard_model = extended_hubbard_model,\n        model_geometry = model_geometry\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Next, we add variational parameters to our model. This first set represents parameters in the determinantal or Slater part of the wavefunction. By initializing the DeterminantalParameters type, a base number of parameters are automatically added to the model, which represent the minimal possible orders. These include charge order in the form of \"mu\" and \"density\", and spin order in the form of \"spin-x\" and \"spin-z\". To flag parameters for optimization, they will need to be added using the add_parameter! method. In this case, we will add an additional parameters in the form of charge and spin stripes.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"We also add variational parameters in the form of Jastrow pseudopotentials, in this case of the \"spin-spin type.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add charge stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"density\",\n        order_type  = \"charge\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Add spin stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"spin-z\",\n        order_type  = \"spin\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"spin-spin\",\n        orbitals        = [1, 2],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Lastly, the model_summary function is used to write a model_summary.toml file, specifying the parameters which will be used in the Hamiltonian that will be simulated.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model, extended_hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_initialize_measurements","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Initialize measurements"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Having initialized both our model and the corresponding model parameters, the next step is to initialize the various measurements we want to make during our VMC simulation. This includes initialization of any correlation measurements we wish to make.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the extended Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, extended_hubbard_model)\n\n    # Initialize equal-time charge correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"density\",\n        pairs = [(1,2)]\n    )\n\n    # Initialize equal-time spin correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"spin-z\",\n        pairs = [(1,2)]\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_setup_vmc","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Set up VMC simulation"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"This section of the code sets up the VMC simulation by initializing the optimizer that will be used during Stochastic Reconfiguration.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_optimize","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Optimize the parameters"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"This next section of the code performs updates to optimize the variational parameters. The structure of this function should be fairly intuitive, mainly consisting of a loop, inside which we perform updates to the particle configuration.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Before the start of the each update loop, we initialize the auxiliary Hamiltonian which is then used to construct the variational wavefunction using the update parametrs from the previous loop.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In the block of code below, N_equil is the number of local updates performed to equilibrate/thermalize the system. Unlike a DQMC simulation, where thermalization updates can be done before any measurements are made, VMC requires the system be thermalized/equilibrated before each measurement. This is because the VMC optimizer is more prone to getting stuck in a local minimum and by equilibrating before each measurement, this reduces this risk.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"The total number of optimization iterations N_opt and the total number of bins N_opt_bins is automatically translated into the correct bin lengths. The bin-averaged measurements are written to file once opt_bin_size measurements are accumulated using the write_measurements! function.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Note that we set opt_step = true in the make_measurements! and write_measurements! functions to ensure that the logarithmic derivatives are being measured that to let the simulation know that the optimizer will handle normalization during this stage of the VMC simulation.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_simulate_system","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Simulate the system"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Now that the variational parameters have been optimzied, we simulate the system using these parameters to obtain clean measurements of any observables. The structure of this section is similar to the optimization block, sans the actual optimizer.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Merge-binned-data","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Merge binned data"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"At this point the simulation is essentially complete, with all updates and measurements having been performed. However, the binned measurement data resides in many separate HDF5 files currently. Here we will merge these separate HDF5 files into a single file containing all the binned data using the merge_bins function. This must be done for both the optimization and simulation bins.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Record-simulation-metadata","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Record simulation metadata"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"Next, we want to calculate the final acceptance rate for the Monte Carlo updates, as well as write the simulation metadata to file, including the contents of the metadata dictionary. This is done using the save_simulation_info function.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#hubbard_square_process_results","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Post-process results"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"In this final section of code we post-process the binned data. This includes calculating the final estimates for the mean and error of all measured observables, which will be written to an HDF5 file using the process_measurements function. Inside this function the binned data gets further re-binned into n_bins, where n_bins is any positive integer satisfying the constraints (N_bins ≥ n_bin) and (N_bins % n_bins == 0). Note that the process_measurements function has many additional keyword arguments that can be used to control the output. For instance, in this example in addition to writing the statistics to an HDF5 file, we also export the statistics to CSV files by setting export_to_csv = true, with additional keyword arguments controlling the formatting of the CSV files. Again, for more information on how to interpret the output refer the Simulation Output Overview page.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \",\n        process_all_equal_time_measurements = true\n    )\n\n    return nothing\nend # end of run_simulation function","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_ladders.html#Execute-script","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"","title":"Execute script"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"VMC simulations are typically run from the command line as jobs on a computing cluster. With this in mind, the following block of code only executes if the Julia script is run from the command line, also reading in additional command line arguments.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),  # Simulation ID.\n        Lx              = parse(Int,     ARGS[2]),  # System size in the x-direction.\n        Ly              = parse(Int,     ARGS[3]),  # System size in the y-direction.\n        t₁′             = parse(Float64, ARGS[4]),  # Next-nearest neighbor hopping amplitude in the first ladder.\n        t₂′             = parse(Float64, ARGS[5]),  # Next-nearest neighbor hopping amplitude in the second ladder.\n        U               = parse(Float64, ARGS[6]),  # Hubbard interaction.\n        V               = parse(Float64, ARGS[7]),  # Extended Hubbard interaction.\n        density         = parse(Float64, ARGS[8]),  # Density of Fermions.\n        ph_transform    = parse(Bool,    ARGS[9]),  # Whether the model is particle-hole transformed.\n        N_equil         = parse(Int,     ARGS[10]), # Number of equilibration/thermalization updates.\n        N_opt           = parse(Int,     ARGS[11]), # Number of optimization steps.\n        N_sim           = parse(Int,     ARGS[12]), # Number of simulation steps.\n        N_opt_bins      = parse(Int,     ARGS[13]), # Number of optimization bins.\n        N_sim_bins      = parse(Int,     ARGS[14])  # Number of simulation bins.\n    )\nend","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"For instance, the command","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"> julia hubbard_ladders.jl 1 16 2 -0.25 -0.25 1.0 0.3 0.875 false 200 3000 6000 300 600","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"page","location":"tutorials/hubbard_ladders.html","page":"3) Interacting Hubbard Ladders with a Stripe Wavefunction","text":"runs a VMC simulation of N=2 interacting ladders at 18-doping with t^prime_1 = t^prime_2 = -025, onsite interaction strength U = 10, and an exteneded interaction strength V = 03. Since there are no pairing terms in the wavefunction, we set ph_transform = false. In the VMC simulation, 200 sweeps through the lattice are be performed to equilibrate the system, while sweeping through 3000 optimiztion iterations and 6000 simulation iterations. Measurements are then written to file 300 times during the optimization and 600 times during simulation, with each bin containing the average of 3000300 = 10 (6000600 = 10) sequential measurements.","title":"3) Interacting Hubbard Ladders with a Stripe Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#1)-Hubbard-Chain-with-an-Antiferromagnetic-Wavefunction","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Download this example as a Julia script.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"This tutorial will work through simulating a repulsive Hubbard model on a one-dimensional (1D) chain using a wavefunction with Néel antiferromagnetic order. The Hubbard Hamiltonian for a 1D chain is given by","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"beginalign\nhatH =  -t sum_i sigma (hatc^dagger_i sigma hatc^phantom dagger_i+1 sigma + rm Hc)\n + U sum_i hatn_iuparrowhatn_idownarrow\n - mu sum_isigma hatn_isigma\nendalign","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"where hatc^dagger_isigma  (hatc^phantom dagger_isigma) creates (annihilates) a spin sigma electron on site i in the lattice, and hatn_sigmai = hatc^dagger_sigmai hatc^phantom dagger_isigma is the spin-sigma electron number operator for site i. In the above Hamiltonian U  0 controls the strength of the on-site Hubbard repulsion.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_import_packages","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Import packages"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Let us begin by importing VariationalMC.jl, and its relevant submodules.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"using VariationalMC\nimport VariationalMC.LatticeUtilities as lu","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"The VariationalMC.jl package re-exports the LatticeUtilities from SmoQyDQMC, which we will use to define the lattice geometry for our model.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"We will also  use the Standard Library packages Random and Printf for random number generation and C-style string formatting, respectively.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"using Random\nusing Printf","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"The entire main body of the simulation we will wrapped in a top-level function named run_simulation that will take as keyword arguments various model and simulation parameters that we may want to change. The function arguments with default values are ones that are typically left unchanged between simulations. The specific meaning of each argument will be discussed in more detail in later sections of the tutorial.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    nup,                    # Number of spin-up Fermions.\n    ndn,                    # Number of spin-down Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_initialize_simulation","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Initialize simulation"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"In this first part of the script we name and initialize our simulation, creating the data folder our simulation results will be written to. This is done by initializing an instances of the SimulationInfo type, and then calling the initialize_datafolder function.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Note that the write_bins_concurrent keyword arguments controls whether or not binned simulation measurement data is written to HDF5 file during the simulation, or held in memory and only written to file once the simulation is complete. Here we decide how to set write_bins_concurrent based on the system size being simulated. This is because when performing simulations of small systems that do not take very long, writing data to file too frequently can sometimes cause network latency problems on clusters and HPC systems. However, for larger systems that take longer to simulate, you are not limited by file IO frequency but rather by available memory, so writing data to file more frequently is preferred in these cases.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_chain_nup%d_ndn%d_U%.2f_L%d\" nup ndn U L\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Initialize-simulation-metadata","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Initialize simulation metadata"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"In this section of the code we record important metadata about the simulation, including initializing the random number generator that will be used throughout the simulation. The important metadata within the simulation will be recorded in the metadata dictionary.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"In the above, sID stands for simulation ID, which is used to distinguish simulations that would otherwise be identical i.e. to distinguish simulations that use the same parameters and are only different in the random seed used to initialize the simulation. A valid sID is any positive integer greater than zero, and is used when naming the data folder the simulation results will be written to. Specifically, the actual data folder created above will be \"$(filepath)/$(datafolder_prefix)-$(sID)\". Note that if you set sID = 0, then it will instead be assigned smallest previously unused integer value. For instance, suppose the directory \"$(filepath)/$(datafolder_prefix)-1\" already exits. Then if you pass sID = 0 to SimulationInfo, then the simulation ID sID = 2 will be used instead, and a directory \"$(filepath)/$(datafolder_prefix)-2\" will be created.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Another useful resource in the documentation is the Simulation Output Overview page, which describes the output written to the data folder generated during a VariationalMC.jl simulation.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Initialize-model","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Initialize model"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"The next step is define the model we wish to simulate. In this example the relevant model parameters are the Hubbard interaction strength U (U) and lattice size L (L).","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"First we define the lattice geometry for our model, relying on the LatticeUtilities package to do so. We define a the unit cell and size of our finite lattice using the UnitCell and Lattice types, respectively. Lastly, we define various instances of the Bond type to represent the the nearest-neighbor and next-nearest-neighbor bonds. All of this information regarding the lattice geometry is then stored in an instance of the ModelGeometry type. Further documentation, with usage examples, for LatticeUtilities package can be found here.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"We also initialize the Canonical Ensemble particle configuration that lives on the lattice. In this case, given the number of spin-up and spin-down particles that we want to live on the lattice, ParticleConfiguration will determine the correct density.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0]],\n        basis_vecs   = [[0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [L],\n        [true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    add_bond!(model_geometry, bond_x)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        nup = nup, ndn = ndn,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Next we specify the non-interacting tight-binding term in our Hamiltonian with the TightBindingModel type.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,           # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x],      # defines hopping.\n        t_mean  = [t]            # defines corresponding mean hopping amplitude.\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Initialize-model-parameters","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Initialize model parameters"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"The next step is to initialize our model parameters given the size of our finite lattice. To clarify, both the TightBindingModel and HubbardModel types are agnostic to the size of the lattice being simulated, defining the model in a translationally invariant way. To do so we need to initialize an instance of the TightBindingParameters and HubbardParameters types.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Note that the initial guess for the chemical potential given in TightBindingModel will be overriden here with the exact one.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Initialize tight-binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard interaction parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Next, we add variational parameters to our model. This first set represents parameters in the determinantal or Slater part of the wavefunction. By initializing the DeterminantalParameters type, a base number of parameters are automatically added to the model, which represent the minimal possible orders. These include charge order in the form of \"mu\" and \"density\", and spin order in the form of \"spin-x\" and \"spin-z\". To flag parameters for optimization, they will need to be added using the add_parameter! method. In this case, we will add the \"spin-z\" parameter for optimization.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"We also add variational parameters in the form of Jastrow pseudopotentials, in this case of the \"density-density type.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add the spin-z variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"spin-z\",\n        order_type      = \"spin\",\n        symmetry        = \"uniform\"\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Lastly, the model_summary function is used to write a model_summary.toml file, specifying the parameters which will be used in the Hamiltonian that will be simulated.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_initialize_measurements","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Initialize measurements"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Having initialized both our model and the corresponding model parameters, the next step is to initialize the various measurements we want to make during our VMC simulation.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_setup_vmc","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Set up VMC simulation"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"This section of the code sets up the VMC simulation by initializing the optimizer that will be used during Stochastic Reconfiguration.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_optimize","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Optimize the parameters"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"This next section of the code performs updates to optimize the variational parameters. The structure of this function should be fairly intuitive, mainly consisting of a loop, inside which we perform updates to the particle configuration.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Before the start of the each update loop, we initialize the auxiliary Hamiltonian which is then used to construct the variational wavefunction using the update parametrs from the previous loop.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"In the block of code below, N_equil is the number of local updates performed to equilibrate/thermalize the system. Unlike a DQMC simulation, where thermalization updates can be done before any measurements are made, VMC requires the system be thermalized/equilibrated before each measurement. This is because the VMC optimizer is more prone to getting stuck in a local minimum and by equilibrating before each measurement, this reduces this risk.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"The total number of optimization iterations N_opt and the total number of bins N_opt_bins is automatically translated into the correct bin lengths. The bin-averaged measurements are written to file once opt_bin_size measurements are accumulated using the write_measurements! function.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Note that we set opt_step = true in the make_measurements! and write_measurements! functions to ensure that the logarithmic derivatives are being measured that to let the simulation know that the optimizer will handle normalization during this stage of the VMC simulation.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_simulate_system","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Simulate the system"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Now that the variational parameters have been optimzied, we simulate the system using these parameters to obtain clean measurements of any observables. The structure of this section is similar to the optimization block, sans the actual optimizer.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Merge-binned-data","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Merge binned data"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"At this point the simulation is essentially complete, with all updates and measurements having been performed. However, the binned measurement data resides in many separate HDF5 files currently. Here we will merge these separate HDF5 files into a single file containing all the binned data using the merge_bins function. This must be done for both the optimization and simulation bins.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Record-simulation-metadata","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Record simulation metadata"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"Next, we want to calculate the final acceptance rate for the Monte Carlo updates, as well as write the simulation metadata to file, including the contents of the metadata dictionary. This is done using the save_simulation_info function.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#hubbard_square_process_results","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Post-process results"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"In this final section of code we post-process the binned data. This includes calculating the final estimates for the mean and error of all measured observables, which will be written to an HDF5 file using the process_measurements function. Inside this function the binned data gets further re-binned into n_bins, where n_bins is any positive integer satisfying the constraints (N_bins ≥ n_bin) and (N_bins % n_bins == 0). Note that the process_measurements function has many additional keyword arguments that can be used to control the output. For instance, in this example in addition to writing the statistics to an HDF5 file, we also export the statistics to CSV files by setting export_to_csv = true, with additional keyword arguments controlling the formatting of the CSV files. Again, for more information on how to interpret the output refer the Simulation Output Overview page.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of run_simulation function","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"tutorials/hubbard_chain.html#Execute-script","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"","title":"Execute script"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"VMC simulations are typically run from the command line as jobs on a computing cluster. With this in mind, the following block of code only executes if the Julia script is run from the command line, also reading in additional command line arguments.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),  # Simulation ID.\n        L               = parse(Int,     ARGS[2]),  # System size.\n        U               = parse(Float64, ARGS[3]),  # Hubbard interaction.\n        nup             = parse(Int,     ARGS[4]),  # Number of spin-up Fermions.\n        ndn             = parse(Int,     ARGS[5]),  # Number of spin-down Fermions.\n        ph_transform    = parse(Bool,    ARGS[6]),  # Whether the model is particle-hole transformed.\n        N_equil         = parse(Int,     ARGS[7]),  # Number of equilibration/thermalization updates.\n        N_opt           = parse(Int,     ARGS[8]),  # Number of optimization steps.\n        N_sim           = parse(Int,     ARGS[9]),  # Number of simulation steps.\n        N_opt_bins      = parse(Int,     ARGS[10]), # Number of optimization bins.\n        N_sim_bins      = parse(Int,     ARGS[11])  # Number of simulation bins.\n    )\nend","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"For instance, the command","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"> julia hubbard_chain.jl 1 8 1.0 4 4 1.0 false 200 3000 6000 300 600","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"page","location":"tutorials/hubbard_chain.html","page":"1) Hubbard Chain with an Antiferromagnetic Wavefunction","text":"runs a VMC simulation of a N = 8 1D Hubbard model at half-filling (4 spin-up, 4 spin-down) with interaction strength U = 10. Since there there are no pairing terms in the wavefunction, we set ph_transform = false. In the VMC simulation, 200 sweeps through the lattice are be performed to equilibrate the system, while sweeping through 3000 optimiztion iterations and 6000 simulation iterations. Measurements are then written to file 300 times during the optimization and 600 times during simulation, with each bin containing the average of 3000300 = 10 (6000600 = 10) sequential measurements.","title":"1) Hubbard Chain with an Antiferromagnetic Wavefunction"},{"category":"section","location":"examples/hubbard_square_mpi.html#Hubbard-Square-with-MPI-Parallelization","page":"Hubbard Square with MPI Parallelization","text":"","title":"Hubbard Square with MPI Parallelization"},{"category":"page","location":"examples/hubbard_square_mpi.html","page":"Hubbard Square with MPI Parallelization","text":"Download this example as a Julia script.","title":"Hubbard Square with MPI Parallelization"},{"category":"page","location":"examples/hubbard_square_mpi.html","page":"Hubbard Square with MPI Parallelization","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a square lattice with an onsite s-wave pairing wavefunction using MPI parallelization.","title":"Hubbard Square with MPI Parallelization"},{"category":"page","location":"examples/hubbard_square_mpi.html","page":"Hubbard Square with MPI Parallelization","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\nusing MPI\n\n# Top-level function to run the simulation\nfunction run_simulation(\n    comm::MPI.Comm;         # MPI communicator.\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_U%.2f_L%d\" density U L\n\n    # Get MPI process ID.\n    pID = MPI.Comm_rank(comm)\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID,\n        pID = pID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions in the .\n    lattice = lu.Lattice(\n        [L, L],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                                      # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y],       # defines hopping.\n        t_mean  = [t, t]                             # defines corresponding mean hopping amplitude.\n    )\n\n    # Define a Hubbard model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )\n\n    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize determinantal parameters.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add the spin-z variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"spin-z\",\n        order_type      = \"spin\",\n        symmetry        = \"uniform\"\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        comm;\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Initialize MPI\n    MPI.Init()\n\n    # Initialize the MPI communicator.\n    comm = MPI.COMM_WORLD\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(\n        comm;\n        sID             = parse(Int,     ARGS[1]),\n        L               = parse(Int,     ARGS[2]),\n        U               = parse(Float64, ARGS[3]),\n        density         = parse(Float64, ARGS[4]),\n        ph_transform    = parse(Bool,    ARGS[5]),\n        N_equil         = parse(Int,     ARGS[6]),\n        N_opt           = parse(Int,     ARGS[7]),\n        N_sim           = parse(Int,     ARGS[8]),\n        N_opt_bins      = parse(Int,     ARGS[9]),\n        N_sim_bins      = parse(Int,     ARGS[10])\n    )\n\n    # Finalize MPI.\n    MPI.Finalize()\nend","title":"Hubbard Square with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#2b)-Square-Hubbard-Model-with-MPI-Parallelization","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Download this example as a Julia script.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"This tutorial will build on the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial, demonstrating how to add parallelization with MPI using the MPI.jl package. By this we mean that each MPI process will act as independent walker, running it's own independent VMC simulation, with the final reported estimates for measured quantities being the average across all walkers.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"The exposition in this tutorial will focus on the changes that need to be made to the 2a) Square Hubbard Model with a Pairing Wavefunction tutorial to introduce MPI parallelization, omitting a more comprehensive discussion of other parts of the code that were included in the previous tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Import-Packages","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Import Packages"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"We now need to import the MPI.jl package as well.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"using VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\nusing MPI","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Specify-simulation-parameters","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Specify simulation parameters"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Here we have introduced the comm argument to the run_simulation function, which is a type exported by the MPI.jl package to facilitate communication and synchronization between the different MPI processes.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"# Top-level function to run simulation.\nfunction run_simulation(\n    comm::MPI.Comm;         # MPI communicator.\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Initialize-simulation","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Initialize simulation"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Now when initializing the SimulationInfo type, we also need to include the MPI process ID pID, which can be retrieved using the MPI.Comm_rank function.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"We also the initialize_datafolder function such that it takes the comm as the first argument. This ensures that all the MPI processes remained synchronized, and none try proceeding beyond this point until the data folder has been initialized.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_U%.2f_L%d\" density U L\n\n    # Get MPI process ID.\n    pID = MPI.Comm_rank(comm)\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID,\n        pID = pID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Initialize-simulation-metadata","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Initialize simulation metadata"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Initialize-model","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Initialize model"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [L, L],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                    # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y],       # defines hopping.\n        t_mean  = [t, t]                  # defines corresponding mean hopping amplitude.\n    )\n\n    # Define a Hubbard model\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Initialize-model-parameters","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Initialize model parameters"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize determinantal parameters.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add the onsite s-wave variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"s-wave\",\n        order_type      = \"pair\",\n        symmetry        = \"uniform\",\n        optimize        = true,\n        orbital         = [1]\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Initialize-measurements","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Initialize measurements"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Set-up-VMC-simulation","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Set up VMC simulation"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Optimize-the-parameters","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Optimize the parameters"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Simulate-the-system","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Simulate the system"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Merge-binned-data","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Merge binned data"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Record-simulation-metadata","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Record simulation metadata"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"No changes need to made to this section of the code from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Post-process-results","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Post-process results"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"The main change we need to make from the previous 2a) Square Hubbard Model with a Pairing Wavefunction tutorial is to call the process_measurements and functions such that the first argument is the comm object, thereby ensuring a parallelized version of each method is called.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Process the simulation results, calculating final error bars for all measurements. writing final statistics to CSV files.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"    process_measurements(\n        comm;\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of run_simulation function","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square_mpi.html#Execute-script","page":"2b) Square Hubbard Model with MPI Parallelization","text":"","title":"Execute script"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Here we first need to initialize MPI using the MPI.Init command. Then, we need to make sure to pass the comm = MPI.COMM_WORLD to the run_simulation function. At the very end of simulation it is good practice to run the MPI.Finalize() function even though it is typically not strictly required.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Initialize MPI\n    MPI.Init()\n\n    # Initialize the MPI communicator.\n    comm = MPI.COMM_WORLD\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(\n        comm;\n        sID             = parse(Int,     ARGS[1]),  # Simulation ID.\n        L               = parse(Int,     ARGS[2]),  # System size.\n        U               = parse(Float64, ARGS[3]),  # Hubbard interaction.\n        density         = parse(Float64, ARGS[4]),  # Density of Fermions.\n        ph_transform    = parse(Bool,    ARGS[5]),  # Whether the model is particle-hole transformed.\n        N_equil         = parse(Int,     ARGS[6]),  # Number of equilibration/thermalization updates.\n        N_opt           = parse(Int,     ARGS[7]),  # Number of optimization steps.\n        N_sim           = parse(Int,     ARGS[8]),  # Number of simulation steps.\n        N_opt_bins      = parse(Int,     ARGS[9]),  # Number of optimization bins.\n        N_sim_bins      = parse(Int,     ARGS[10])  # Number of simulation bins.\n    )\n\n    # Finalize MPI.\n    MPI.Finalize()\nend","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Here is an example of what the command to run this script might look like:","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"mpiexecjl -n 8 julia hubbard_square_mpi.jl 1 4 2.0 0.875 true 200 3000 6000 300 600","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"This will initialize 8 MPI processes, each running and independent simulation using a different random seed the final results arrived at by averaging over all 16 walkers. Here mpiexecjl is the MPI executable that can be easily install using the directions found here in the MPI.jl documentation. However, you can substitute a different MPI executable here if one is already configured on your system.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"Also, when submitting jobs via SLURM on a High-Performance Computing (HPC) cluster, if a default MPI executable is already configured on the system, as is frequently the case, then the script can likely be run inside the *.sh job file using the srun command:","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"srun julia hubbard_square_mpi.jl 1 4 2.0 0.875 true 200 3000 6000 300 600","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"page","location":"tutorials/hubbard_square_mpi.html","page":"2b) Square Hubbard Model with MPI Parallelization","text":"The srun command should automatically detect the number of available cores requested by the job and run the script using the MPI executable with the appropriate number of processes.","title":"2b) Square Hubbard Model with MPI Parallelization"},{"category":"section","location":"tutorials/hubbard_square.html#2a)-Square-Hubbard-Model-with-a-Pairing-Wavefunction","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Download this example as a Julia script.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In this tutorial, we will work through simulating a repulsive Hubbard model on a square lattice using a variational wavefunction with s-wave pairing. The Hubbard Hamiltonian for a square lattice given by","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"beginalign\nhatH =  -t sum_langle i j rangle sigma (hatc^dagger_i sigma hatc^phantom dagger_j sigma + rm Hc)\n + U sum_i hatn_iuparrowhatn_idownarrow\n - mu sum_isigma hatn_isigma\nendalign","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"where hatc^dagger_isigma  (hatc^phantom dagger_isigma) creates (annihilates) a spin sigma electron on site i in the lattice, and hatn_isigma = hatc^dagger_isigma hatc^phantom dagger_isigma is the spin-sigma electron number operator for site i. In the above Hamiltonian U  0 controls the strength of the on-site Hubbard repulsion.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_import_packages","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Import packages"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Let us begin by importing VariationalMC.jl, and its relevant submodules.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"using VariationalMC\nimport VariationalMC.LatticeUtilities as lu","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"The VariationalMC.jl package re-exports the LatticeUtilities from SmoQyDQMC, which we will use to define the lattice geometry for our model.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"We will also  use the Standard Library packages Random and Printf for random number generation and C-style string formatting, respectively.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"using Random\nusing Printf","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Specify-simulation-parameters","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Specify simulation parameters"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"The entire main body of the simulation we will wrapped in a top-level function named run_simulation that will take as keyword arguments various model and simulation parameters that we may want to change. The function arguments with default values are ones that are typically left unchanged between simulations. The specific meaning of each argument will be discussed in more detail in later sections of the tutorial.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"# Top-level function to run simulation.\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_initialize_simulation","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Initialize simulation"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In this first part of the script we name and initialize our simulation, creating the data folder our simulation results will be written to. This is done by initializing an instances of the SimulationInfo type, and then calling the initialize_datafolder function.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Note that the write_bins_concurrent keyword arguments controls whether or not binned simulation measurement data is written to HDF5 file during the simulation, or held in memory and only written to file once the simulation is complete. Here we decide how to set write_bins_concurrent based on the system size being simulated. This is because when performing simulations of small systems that do not take very long, writing data to file too frequently can sometimes cause network latency problems on clusters and HPC systems. However, for larger systems that take longer to simulate, you are not limited by file IO frequency but rather by available memory, so writing data to file more frequently is preferred in these cases.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_U%.2f_L%d\" density U L","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Initialize simulation info.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        write_bins_concurrent = (L > 10),\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Initialize-simulation-metadata","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Initialize simulation metadata"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In this section of the code we record important metadata about the simulation, including initializing the random number generator that will be used throughout the simulation. The important metadata within the simulation will be recorded in the metadata dictionary.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In the above, sID stands for simulation ID, which is used to distinguish simulations that would otherwise be identical i.e. to distinguish simulations that use the same parameters and are only different in the random seed used to initialize the simulation. A valid sID is any positive integer greater than zero, and is used when naming the data folder the simulation results will be written to. Specifically, the actual data folder created above will be \"$(filepath)/$(datafolder_prefix)-$(sID)\". Note that if you set sID = 0, then it will instead be assigned smallest previously unused integer value. For instance, suppose the directory \"$(filepath)/$(datafolder_prefix)-1\" already exits. Then if you pass sID = 0 to SimulationInfo, then the simulation ID sID = 2 will be used instead, and a directory \"$(filepath)/$(datafolder_prefix)-2\" will be created.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Another useful resource in the documentation is the Simulation Output Overview page, which describes the output written to the data folder generated during a VariationalMC.jl simulation.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Initialize-model","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Initialize model"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"The next step is define the model we wish to simulate. In this example the relevant model parameters are the Hubbard interaction strength U (U) and lattice size L (L).","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"First we define the lattice geometry for our model, relying on the LatticeUtilities package to do so. We define a the unit cell and size of our finite lattice using the UnitCell and Lattice types, respectively. Lastly, we define various instances of the Bond type to represent the the nearest-neighbor bonds. All of this information regarding the lattice geometry is then stored in an instance of the ModelGeometry type. Further documentation, with usage examples, for LatticeUtilities package can be found here.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"We also initialize the Canonical Ensemble particle configuration that lives on the lattice. In this case, given the total Fermion density that we want to live on the lattice, ParticleConfiguration will determine the correct total number of particles and the correct number of spin-up and spin-down particles.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [L, L],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Next we specify the non-interacting tight-binding term in our Hamiltonian with the TightBindingModel type.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                    # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y],       # defines hopping.\n        t_mean  = [t, t]                  # defines corresponding mean hopping amplitude.\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Then, we define the Hubbard interaction with the HubbardModel type.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Initialize-model-parameters","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Initialize model parameters"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"The next step is to initialize our model parameters given the size of our finite lattice. To clarify, both the TightBindingModel and HubbardModel types are agnostic to the size of the lattice being simulated, defining the model in a translationally invariant way. To do so we need to initialize an instance of the TightBindingParameters and HubbardParameters types.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Note that the initial guess for the chemical potential given in TightBindingModel will be overriden here with the exact one.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Next, we add variational parameters to our model. This first set represents parameters in the determinantal or Slater part of the wavefunction. By initializing the DeterminantalParameters type, a base number of parameters are automatically added to the model, which represent the minimal possible orders. These include charge order in the form of \"mu\" and \"density\", and spin order in the form of \"spin-x\" and \"spin-z\". To flag parameters for optimization, they will need to be added using the add_parameter! method. In this case, we will add an additional parameter in the form of s-wave pairing.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"We also add variational parameters in the form of Jastrow pseudopotentials, in this case of the \"density-density type.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add the onsite s-wave variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"s-wave\",\n        order_type      = \"pair\",\n        symmetry        = \"uniform\",\n        optimize        = true,\n        orbital         = [1]\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Lastly, the model_summary function is used to write a model_summary.toml file, specifying the parameters which will be used in the Hamiltonian that will be simulated.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_initialize_measurements","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Initialize measurements"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Having initialized both our model and the corresponding model parameters, the next step is to initialize the various measurements we want to make during our VMC simulation.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_setup_vmc","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Set up VMC simulation"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"This section of the code sets up the VMC simulation by initializing the optimizer that will be used during Stochastic Reconfiguration.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_optimize","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Optimize the parameters"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"This next section of the code performs updates to optimize the variational parameters. The structure of this function should be fairly intuitive, mainly consisting of a loop, inside which we perform updates to the particle configuration.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Before the start of the each update loop, we initialize the auxiliary Hamiltonian which is then used to construct the variational wavefunction using the update parametrs from the previous loop.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In the block of code below, N_equil is the number of local updates performed to equilibrate/thermalize the system. Unlike a DQMC simulation, where thermalization updates can be done before any measurements are made, VMC requires the system be thermalized/equilibrated before each measurement. This is because the VMC optimizer is more prone to getting stuck in a local minimum and by equilibrating before each measurement, this reduces this risk.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"The total number of optimization iterations N_opt and the total number of bins N_opt_bins is automatically translated into the correct bin lengths. The bin-averaged measurements are written to file once opt_bin_size measurements are accumulated using the write_measurements! function.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Note that we set opt_step = true in the make_measurements! and write_measurements! functions to ensure that the logarithmic derivatives are being measured that to let the simulation know that the optimizer will handle normalization during this stage of the VMC simulation.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_simulate_system","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Simulate the system"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Now that the variational parameters have been optimzied, we simulate the system using these parameters to obtain clean measurements of any observables. The structure of this section is similar to the optimization block, sans the actual optimizer.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Merge-binned-data","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Merge binned data"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"At this point the simulation is essentially complete, with all updates and measurements having been performed. However, the binned measurement data resides in many separate HDF5 files currently. Here we will merge these separate HDF5 files into a single file containing all the binned data using the merge_bins function. This must be done for both the optimization and simulation bins.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Record-simulation-metadata","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Record simulation metadata"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"Next, we want to calculate the final acceptance rate for the Monte Carlo updates, as well as write the simulation metadata to file, including the contents of the metadata dictionary. This is done using the save_simulation_info function.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#hubbard_square_process_results","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Post-process results"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"In this final section of code we post-process the binned data. This includes calculating the final estimates for the mean and error of all measured observables, which will be written to an HDF5 file using the process_measurements function. Inside this function the binned data gets further re-binned into n_bins, where n_bins is any positive integer satisfying the constraints (N_bins ≥ n_bin) and (N_bins % n_bins == 0). Note that the process_measurements function has many additional keyword arguments that can be used to control the output. For instance, in this example in addition to writing the statistics to an HDF5 file, we also export the statistics to CSV files by setting export_to_csv = true, with additional keyword arguments controlling the formatting of the CSV files. Again, for more information on how to interpret the output refer the Simulation Output Overview page.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of run_simulation function","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"tutorials/hubbard_square.html#Execute-script","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"","title":"Execute script"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"VMC simulations are typically run from the command line as jobs on a computing cluster. With this in mind, the following block of code only executes if the Julia script is run from the command line, also reading in additional command line arguments.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),  # Simulation ID.\n        L               = parse(Int,     ARGS[2]),  # System size.\n        U               = parse(Float64, ARGS[3]),  # Hubbard interaction.\n        density         = parse(Float64, ARGS[4]),  # Density of Fermions.\n        ph_transform    = parse(Bool,    ARGS[5]),  # Whether the model is particle-hole transformed.\n        N_equil         = parse(Int,     ARGS[6]),  # Number of equilibration/thermalization updates.\n        N_opt           = parse(Int,     ARGS[7]),  # Number of optimization steps.\n        N_sim           = parse(Int,     ARGS[8]),  # Number of simulation steps.\n        N_opt_bins      = parse(Int,     ARGS[9]),  # Number of optimization bins.\n        N_sim_bins      = parse(Int,     ARGS[10])  # Number of simulation bins.\n    )\nend","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"For instance, the command","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"> julia hubbard_square.jl 1 4 2.0 0.875 true 200 3000 6000 300 600","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"page","location":"tutorials/hubbard_square.html","page":"2a) Square Hubbard Model with a Pairing Wavefunction","text":"runs a VMC simulation of a N = 4 times 4 square Hubbard model at 18-doping with interaction strength U = 20. Becuase we added a pairing wavefunction, we ensure that ph_transform = true. In the VMC simulation, 200 sweeps through the lattice are be performed to equilibrate the system, while sweeping through 3000 optimiztion iterations and 6000 simulation iterations. Measurements are then written to file 300 times during the optimization and 600 times during simulation, with each bin containing the average of 3000300 = 10 (6000600 = 10) sequential measurements.","title":"2a) Square Hubbard Model with a Pairing Wavefunction"},{"category":"section","location":"api.html#API","page":"API","text":"","title":"API"},{"category":"section","location":"api.html#Simulation-Information-Type-and-Methods","page":"API","text":"","title":"Simulation Information Type and Methods"},{"category":"type","location":"api.html#VariationalMC.SimulationInfo","page":"API","text":"SimulationInfo\n\nContains identification information about simulation, including the location data is written to, the simulation ID, and MPI process ID, and whether this simulation started a new simulation or resumed a previous simulation.\n\nFields\n\nfilepath::String: File path to where data folder lives.\ndatafolder_prefix: Prefix for the data folder name.\ndatafolder_name::String: The data folder name, given by $(datafolder_prefix)_$(sID).\ndatafolder::String: The data folder, including filepath, given by joinpath(filepath, datafolder_name).\npID::Int: MPI process ID, defaults to 0 if MPI not being used.\nsID::Int: Simulation ID.\nbin_files::Vector{Vector{UInt8}}: Represents the HDF5 files containing the binned data.\nresuming::Bool: Whether current simulation is resuming a previous simulation (true) or starting a new one (false).\nvariationalmc_version::VersionNumber: Version of VariationalMC.jl used in simulation.\n\n\n\n\n\n","title":"VariationalMC.SimulationInfo"},{"category":"method","location":"api.html#VariationalMC.SimulationInfo-Tuple{}","page":"API","text":"SimulationInfo(;\n    # KEYWORD ARGUMENTS\n    datafolder_prefix::String,\n    filepath::String = \".\",\n    sID::Int=0,\n    pID::Int=0\n)\n\nInitialize and return in instance of the type SimulationInfo.\n\n\n\n\n\n","title":"VariationalMC.SimulationInfo"},{"category":"function","location":"api.html#VariationalMC.initialize_datafolder","page":"API","text":"initialize_datafolder(comm::MPI.Comm, sim_info::SimulationInfo)\n\ninitialize_datafolder(sim_info::SimulationInfo)\n\nInitialize sim_info.datafolder directory if it does not already exist. If comm::MPI.Comm is passed as the first argument, this this function will synchronize all the MPI processes, ensuring that none proceed beyond this function call until the data folder that results will be written to is successfully initialized.\n\n\n\n\n\n","title":"VariationalMC.initialize_datafolder"},{"category":"function","location":"api.html#VariationalMC.model_summary","page":"API","text":"model_summary(;\n    simulation_info::SimulationInfo,\n    model_geometry::ModelGeometry,\n    particle_configuration::ParticleConfiguration,\n    tight_binding_model::TightBindingModel,\n    interactions::Union{Tuple,Nothing} = nothing,\n    parameters::Union{Tuple, Nothing} = nothing\n)\n\nWrite model to summary to file. This includes information on the canonical ensemble configuration, tight binding model, interactions, and variational parameters.\n\n\n\n\n\n","title":"VariationalMC.model_summary"},{"category":"section","location":"api.html#Model-Geometry-Type-and-Methods","page":"API","text":"","title":"Model Geometry Type and Methods"},{"category":"type","location":"api.html#VariationalMC.ModelGeometry","page":"API","text":"ModelGeometry{D, T<:AbstractFloat, N}\n\nContains all the information defining the lattice geometry for the model in D spatial dimensions.\n\nComment\n\nThe bond ID associated with a bond::Bond{D} corresponds to the index associated with it into the bonds vector field.\n\nFields\n\nunit_cell::UnitCell{D,T,N}: Defines unit cell.\nlattice::Lattice{D}: Defines finite lattice extent.\nbonds::Vector{Bond{D}}: All available bond definitions in simulation, with vector indices giving the bond ID.\n\n\n\n\n\n","title":"VariationalMC.ModelGeometry"},{"category":"method","location":"api.html#VariationalMC.ModelGeometry-Union{Tuple{D}, Tuple{UnitCell{D}, Lattice{D}}} where D","page":"API","text":"ModelGeometry(  unit_cell::UnitCell, \n                lattice::Lattice ) where {D}\n\nInitialize and return a ModelGeometry instance. Defines a \"trivial\" bond definition for each orbital in the unit cell that connects an orbital to itself.\n\n\n\n\n\n","title":"VariationalMC.ModelGeometry"},{"category":"function","location":"api.html#VariationalMC.add_bond!","page":"API","text":"add_bond!(  model_geometry::ModelGeometry{D,T}, \n            bond::Bond{D}) where {D, T}\n\nAdd bond definition to model_geometry, returning the bond ID i.e. the index to bond in the vector model_geometry.bonds. This method first checks that bond is not already defined. If it is this method simply returns the corresponding bond ID. If bond is not already defined, then it is appended to the vector model_geometry.bonds.\n\n\n\n\n\n","title":"VariationalMC.add_bond!"},{"category":"function","location":"api.html#VariationalMC.get_bond_id","page":"API","text":"get_bond_id(model_geometry::ModelGeometry{D,T}, bond::Bond{D}) where {D, T}\n\nReturn the bond ID associated with the bond defintion bond, returning bond_id=0 if the it is not a recorded bond.\n\n\n\n\n\n","title":"VariationalMC.get_bond_id"},{"category":"section","location":"api.html#Particle-Configuration-Type-and-Methods","page":"API","text":"","title":"Particle Configuration Type and Methods"},{"category":"type","location":"api.html#VariationalMC.ParticleConfiguration","page":"API","text":"ParticleConfiguration( \n    density::AbstractFloat; \n    # KEYWORD ARGUMENTS\n    model_geometry::ModelGeometry,\n    particle_hole_transform::Bool \n) where {E<:AbstractFloat}\n\nGiven an initial particle density, initialize and return an instance of the type ParticleConfiguration.\n\ndensity::AbstractFloat: desired electronic density.\n\nKEYWORD ARGUMENTS\n\nmodel_geometry::ModelGeometry: contains unit cell and lattice quantities. \nph_transform::Bool: Whether configuration will be particle-hole transformed.\n\n\n\n\n\nParticleConfiguration( \n    Np::Int;\n    # KEYWORD ARGUMENTS\n    model_geometry::ModelGeometry,\n    particle_hole_transform::Bool \n) where {I<:Integer}\n\nGiven an initial particle number, initialize and return an instance of the type ParticleConfiguration.\n\nNp::Int: total number of particles. \n\nKEYWORD ARGUMENTS\n\nmodel_geometry::ModelGeometry: contains unit cell and lattice quantities. \nph_transform::Bool: Whether configuration is particle-hole transformed. \n\n\n\n\n\nParticleConfiguration(;\n    # KEYWORD ARGUMENTS\n    nup::Int, \n    ndn::Int,\n    model_geometry::ModelGeometry, \n    particle_hole_transform::Bool \n) where {I<:Integer}\n\nGiven a certain number of spin-up and spin-down particles, initialize and return an instance of the type ParticleConfiguration.\n\nKEYWORD ARGUMENTS\n\nnup::Int: number of spin-up fermions.\nndn::Int: number of spin-down fermions.\nmodel_geometry::ModelGeometry: contains unit cell and lattice quantities.\nph_transform::Bool: Whether configuration is particle-hole transformed. \n\n\n\n\n\n","title":"VariationalMC.ParticleConfiguration"},{"category":"method","location":"api.html#VariationalMC.ParticleConfiguration-Tuple{}","page":"API","text":"ParticleConfiguration(;\n    # KEYWORD ARGUMENTS\n    nup::Int, \n    ndn::Int,\n    model_geometry::ModelGeometry, \n    particle_hole_transform::Bool \n) where {I<:Integer}\n\nGiven a certain number of spin-up and spin-down particles, initialize and return an instance of the type ParticleConfiguration.\n\nKEYWORD ARGUMENTS\n\nnup::Int: number of spin-up fermions.\nndn::Int: number of spin-down fermions.\nmodel_geometry::ModelGeometry: contains unit cell and lattice quantities.\nph_transform::Bool: Whether configuration is particle-hole transformed. \n\n\n\n\n\n","title":"VariationalMC.ParticleConfiguration"},{"category":"section","location":"api.html#Tight-Binding-Model-and-Parameters","page":"API","text":"","title":"Tight-Binding Model and Parameters"},{"category":"type","location":"api.html#VariationalMC.TightBindingModel","page":"API","text":"TightBindingModel{T<:Number, E<:AbstractFloat, D}\n\nDefines a non-interacting tight binding model in D dimensions. \n\nFields\n\nμ::E: Initial value of the chemical potential.\nt_bond_ids::Vector{Int}: The bond ID for each bond/hopping definition.\nt_bonds::Vector{Bond{D}}: Bond definition for each type of hopping in the tight binding model.\nt_mean::Vector{T}: Mean hopping energy for each type of hopping.\nη::Vector{E}: Relative twist angle for each lattice vector direction d in 1 D such that eta_d in 0 1)\nexpniϕ::Vector{T}: Twist angle phase exp(i n_d phi_d) for each hopping.\n\n\n\n\n\n","title":"VariationalMC.TightBindingModel"},{"category":"method","location":"api.html#VariationalMC.TightBindingModel-Tuple{}","page":"API","text":"TightBindingModel(;\n    # KEYWORD ARGUMENTS\n    model_geometry::ModelGeometry{D,E,N},\n    μ::E,\n    t_bonds::Vector{Bond{D}} = Bond{ndims(model_geometry.unit_cell)}[],\n    t_mean::Vector{T} = T[],\n    η::Union{Vector{E},Nothing} = nothing\n) where {T<:Number, E<:AbstractFloat, D, N}\n\nInitialize and return an instance of TightBindingModel, also adding/recording the bond defintions t_bonds to the ModelGeometry instance model_geometry.\n\n\n\n\n\n","title":"VariationalMC.TightBindingModel"},{"category":"type","location":"api.html#VariationalMC.TightBindingParameters","page":"API","text":"TightBindingParameters{T<:Number, E<:AbstractFloat}\n\nA mutable struct containing all the parameters needed to characterize a finite tight-binding Hamiltonian for a single spin species sigma on a finite lattice with periodic boundary conditions of the form\n\nhatH_0sigma=-sum_langle ijrangle(t_ij hatc_sigmai^daggerhatc_sigmaj+textrmhc)\n\nwhere hatc_sigmai^dagger is the fermion creation operator for an electron with spin sigma on orbital i t_ij are the hopping energies, and mu is the chemical potential.\n\nFields\n\nμ::E: The chemical potential mu\nconst t::Vector{T}: The hopping energy t_ij associated with each pair of neighboring orbitals connected by a bond in the lattice.\nconst neighbor_table::Matrix{Int}: Neighbor table containing all pairs of orbitals in the lattices connected by a bond, with a non-zero hopping energy between them.\nconst neighbor_map::Dict: Dictionary containing all sites in the lattice, mapping them to their valid neighboring sites and the bonds between them.\nconst bond_ids::Vector{Int}: The bond ID definitions that define the types of hopping in the lattice.\nconst bond_slices::Vector{UnitRange{Int}}: Slices of neighbor_table corresponding to given bond ID i.e. the neighbors neighbor_table[:,bond_slices[i]] corresponds the bond_ids[i] bond defintion.\nconst norbital::Int: Number of orbitals per unit cell.\n\n\n\n\n\n","title":"VariationalMC.TightBindingParameters"},{"category":"method","location":"api.html#VariationalMC.TightBindingParameters-Tuple{}","page":"API","text":"TightBindingParameters(;\n    # KEYWORD ARGUMENTS\n    tight_binding_model::TightBindingModel{T,E,D},\n    particle_configuration::ParticleConfiguration{E,I},\n    model_geometry::ModelGeometry{D,E}\n) where {T,E,D}\n\nInitialize and return an instance of TightBindingParameters.\n\n\n\n\n\n","title":"VariationalMC.TightBindingParameters"},{"category":"function","location":"api.html#VariationalMC.measure_hopping_energy","page":"API","text":"measure_hopping_energy(\n    detwf::DeterminantalWavefunction{T},\n    tight_binding_parameters::TightBindingParameters{T,E},\n    pconfig::Vector{Int},\n    hopping_id::Int,\n    N::Int,\n    ph_transform::Bool;\n    # KEYWORD ARGUMENTS\n    model_geometry::ModelGeometry = model_geometry,\n    jas_factors::Union{Tuple{<:AbstractJastrowFactor{T}},Nothing} = nothing,\n    jas_parameters::Union{Tuple{JastrowParameters{T}},Nothing} = nothing\n) where {T<:Number, E<:AbstractFloat}\n\nCalculate the average hopping energy -langle t_langle ij rangle hatc^dagger_sigmai hatc_sigmaj + rm hc rangle.\n\n\n\n\n\n","title":"VariationalMC.measure_hopping_energy"},{"category":"section","location":"api.html#Hubbard-Model-and-Parameters","page":"API","text":"","title":"Hubbard Model and Parameters"},{"category":"type","location":"api.html#VariationalMC.HubbardModel","page":"API","text":"HubbardModel{T<:AbstractFloat}\n\nA type to represent a multi-orbital Hubbard model; specifically, the particle-hole asymmetric form for the Hubbard interaction\n\nhatH_U=sum_mathbfinuU_numathbfihatn_uparrownumathbfihatn_downarrownumathbfi\n\nwhere mathbfi specifies the unit cell, and nu denotes the orbital in the unit cell.\n\nFields\n\nU_orbital_ids::Vector{Int}: Orbital species/IDs in unit cell with finite Hubbard interaction.\nU_mean::Vector{T}: Average Hubbard interaction strength U_nu for a given orbital species in the lattice.\n\n\n\n\n\n","title":"VariationalMC.HubbardModel"},{"category":"method","location":"api.html#VariationalMC.HubbardModel-Tuple{}","page":"API","text":"HubbardModel(;\n    # KEYWORD ARGUMENTS\n    U_orbital::AbstractVector{Int},\n    U_mean::AbstractVector{T}\n) where {T<:AbstractFloat}\n\nInitialize and return an instance of the type HubbardModel.\n\nKeyword Arguments\n\nU_orbital::Vector{Int}: Orbital species/IDs in unit cell with finite Hubbard interaction.\nU_mean::Vector{T}: Average Hubbard interaction strength U_nu for a given orbital species in the lattice.\n\n\n\n\n\n","title":"VariationalMC.HubbardModel"},{"category":"type","location":"api.html#VariationalMC.HubbardParameters","page":"API","text":"HubbardParameters{T<:AbstractFloat}\n\nHubbard parameters for finite lattice.\n\nFields\n\nU::Vector{T}: On-site Hubbard interaction for each site with finite Hubbard interaction.\nsites::Vector{Int}: Site index associated with each finite Hubbard U interaction.\norbital_ids::Vector{Int}: Orbital ID/species in unit cell with finite Hubbard interaction.\n\n\n\n\n\n","title":"VariationalMC.HubbardParameters"},{"category":"method","location":"api.html#VariationalMC.HubbardParameters-Tuple{}","page":"API","text":"HubbardParameters(;\n    hubbard_model::HubbardModel{T},\n    model_geometry::ModelGeometry{D,T}\n) where {D, T<:AbstractFloat}\n\nInitialize an instance of HubbardParameters.\n\n\n\n\n\n","title":"VariationalMC.HubbardParameters"},{"category":"type","location":"api.html#VariationalMC.ExtendedHubbardModel","page":"API","text":"ExtendedHubbardModel{T<:AbstractFloat}\n\nA type to represent extended Hubbard interactions; specifically, the particle-hole asymmetric form of the extended Hubbard interaction\n\nbeginalign*\nhatH_V = sum_mathbfjmathbfrnuetaV_(mathbfj+mathbfrnu)(mathbfjeta)  hatn_mathbfj+mathbfrnuhatn_mathbfjeta \n    = sum_mathbfjmathbfrnuetaV_(mathbfj+mathbfrnu)(mathbfjeta)  biggtfrac12(hatn_mathbfj+mathbfrnu+hatn_mathbfjeta-2)^2-1 \n     -hatn_mathbfj+mathbfrnuuparrowhatn_mathbfj+mathbfrnudownarrow-hatn_mathbfjetauparrowhatn_mathbfjetadownarrow+tfrac32hatn_mathbfj+mathbfrnu+tfrac32hatn_mathbfjetabigg\nendalign*\n\nwhere mathbfj specifies a unit cell in the lattice, mathbfr is a displacement in units, and nu and eta specify the orbital in a given unit cell. Here, hatn_mathbfjeta = (hatn_uparrowmathbfjeta + hatn_downarrowmathbfjeta) is the electron number operator for orbital eta in unit cell mathbfj in the lattice. V_(mathbfj+mathbfrnu)(mathbfjeta) controls the strength of the extended Hubbard interaction between orbital eta in unit cell mathbfj and orbital nu in unit cell mathbfj+mathbfr.\n\nFields\n\nV_bond_ids::Vector{Int}: Bond IDs specifying bond definition that separates a pair of orbitals with an extended Hubbard interaction between them.\nV_mean::Vector{T}: Average extended Hubbard interaction strength V_(mathbfj+mathbfrnu)(mathbfjeta) associated with bond definition.\n\n\n\n\n\n","title":"VariationalMC.ExtendedHubbardModel"},{"category":"method","location":"api.html#VariationalMC.ExtendedHubbardModel-Tuple{}","page":"API","text":"ExtendedHubbardModel(;\n    # KEYWORD ARGUMENTS\n    model_geometry::ModelGeometry{D,T},\n    V_bond::Vector{Bond{D}},\n    V_mean::Vector{T},\n) where {T<:AbstractFloat, D}\n\nInitialize and return an instance of the type ExtendedHubbardModel.\n\n\n\n\n\n","title":"VariationalMC.ExtendedHubbardModel"},{"category":"type","location":"api.html#VariationalMC.ExtendedHubbardParameters","page":"API","text":"ExtendedHubbardParameters{T<:AbstractFloat}\n\nExtended Hubbard interaction parameters for finite lattice.\n\nFields\n\nV::Vector{T}: Extended Hubbard interaction strength for each pair neighbors in the lattice.\nneighbor_table::Matrix{Int}: Neighbor table for extended Hubbard interactions on lattice.\nbond_ids::Vector{Int}: Bond IDs used to define extended Hubbard interactions.\n\n\n\n\n\n","title":"VariationalMC.ExtendedHubbardParameters"},{"category":"method","location":"api.html#VariationalMC.ExtendedHubbardParameters-Tuple{}","page":"API","text":"ExtendedHubbardParameters(;\n    # KEYWORD ARGUMENTS\n    extended_hubbard_model::ExtendedHubbardModel{T},\n    model_geometry::ModelGeometry{D,T}\n) where {D, T<:AbstractFloat}\n\nInitialize an instance of the ExtendedHubbardParameters type.\n\n\n\n\n\n","title":"VariationalMC.ExtendedHubbardParameters"},{"category":"function","location":"api.html#VariationalMC.measure_hubbard_energy","page":"API","text":"measure_hubbard_energy(\n    hubbard_parameters::HubbardParameters{E},\n    hubbard_id::Int,\n    pconfig::Vector{Int},\n    ph_transform::Bool\n) where {E<:AbstractFloat}\n\nCalculates the average Hubbard energy U langle hatn_uparrow hatn_downarrow rangle.\n\nARGUMENTS\n\nhubbard_parameters::HubbardParameters{E}: Instance of HubbardParameters.\nhubbard_id::Int: ID of the Hubbard interaction.\npconfig::Vector{Int}: Current particle configuration.\nph_transform::Bool: Whether the model is particle-hole transformed.\n\n\n\n\n\n","title":"VariationalMC.measure_hubbard_energy"},{"category":"function","location":"api.html#VariationalMC.measure_ext_hubbard_energy","page":"API","text":"measure_ext_hubbard_energy(\n    extended_hubbard_parameters::ExtendedHubbardParameters{E},\n    ext_hubbard_id::Int,\n    pconfig::Vector{Int},\n    ph_transform::Bool\n) where {E<:AbstractFloat}\n\nCalculates the average extended Hubbard interaction energy V hatn_i hatn_j.\n\nARGUMENTS\n\nextended_hubbard_parameters::ExtendedHubbardParameters{E}: Instance of ExtendedHubbardParameters.\next_hubbard_id::Int: ID of the extended Hubbard interaction.\npconfig::Vector{Int}: Current particle configuration.\nph_transform::Bool: Whether model is particle-hole transformed.\n\n\n\n\n\n","title":"VariationalMC.measure_ext_hubbard_energy"},{"category":"section","location":"api.html#Variational-Parameters","page":"API","text":"","title":"Variational Parameters"},{"category":"page","location":"api.html","page":"API","text":"Variational Parameter Names\nDeterminantal Parameters\nJastrow Parameters","title":"API"},{"category":"section","location":"api.html#Variational-Parameter-Names","page":"API","text":"","title":"Variational Parameter Names"},{"category":"constant","location":"api.html#VariationalMC.DETERMINANTAL_PARAMETERS","page":"API","text":"DETERMINANTAL_PARAMETERS = Base.ImmutableDict(\n    \"μ\"             => \"charge\", \n    \"density\"       => \"charge\", \n    \"spin-x\"        => \"spin\", \n    \"spin-z\"        => \"spin\", \n    \"s-wave\"        => \"pair\", \n    \"d-wave\"        => \"pair\"\n)\n\nList of all determinantal parameters that can be added to the wavefunction, along with the corresponding ordering type.\n\n\n\n\n\n","title":"VariationalMC.DETERMINANTAL_PARAMETERS"},{"category":"constant","location":"api.html#VariationalMC.JASTROW_PARAMETERS","page":"API","text":"JASTROW_PARAMETERS = Base.ImmutableDict(\n    \"electron-electron\" => [\"density-density\", \"spin-spin\"],\n    \"phonon-phonon\"   => [\"density-density\", \"displacement-displacement\"],\n    \"electron-phonon\"   => [\"density-density\", \"density-displacement\"]\n)\n\nList of all Jastrow parameters that can be added, along with the corresponding ordering type.\n\n\n\n\n\n","title":"VariationalMC.JASTROW_PARAMETERS"},{"category":"section","location":"api.html#Determinantal-Parameters","page":"API","text":"","title":"Determinantal Parameters"},{"category":"type","location":"api.html#VariationalMC.DeterminantalParameters","page":"API","text":"DeterminantalParameters{S<:AbstractString, T<:Number}\n\nDeterminantal parameters allowing for different types of ordering. This include charge order in the form of \"μ\"  and density, spin order in the form of \"spin-x\" and \"spin-x\", and pairing in the form of \"s-wave\"  and \"d-wave\".\n\nFields\n\norder_type::Vector{S}: The class of each order parameter, being \"charge\", \"spin\", or \"pair\".\nparam_name::Vector{S}: Specific order parameters names.\nsymmetry::Vector{S}: Symmetry of each order parameter, being \"uniform\" or \"site-dependent\".\np::Vector{Vector{T}}: Vector of order parameters.\nqₚ::Vector{AbstractFloat}: Center-of-mass pairing momentum when introducing site-dependent pairing. \noptimize::Vector{Bool}: Determines whether an order parameter will be optimized.\norbital::Vector{Int}: All orbital species where each order parameter applies.\n\n\n\n\n\n","title":"VariationalMC.DeterminantalParameters"},{"category":"method","location":"api.html#VariationalMC.DeterminantalParameters-Tuple{}","page":"API","text":"DeterminantalParameters(;\n    tight_binding_parameters::TightBindingParameters{T},\n    model_geometry::ModelGeometry{D,T},\n    rng::AbstractRNG\n) where {D, T<:AbstractFloat}\n\nInitialize and return an instance of `DeterminantalParameters'.\n\n\n\n\n\n","title":"VariationalMC.DeterminantalParameters"},{"category":"function","location":"api.html#VariationalMC.add_parameter!","page":"API","text":"add_parameter!(\n    determinantal_parameters::DeterminantalParameters;\n    param_name::Union{S, Nothing} = nothing,\n    order_type::Union{S, Nothing} = nothing,\n    symmetry::Union{S, Nothing} = nothing,\n    optimize::Union{Bool, Nothing} = nothing,\n    orbital::Union{Vector{Int}, Nothing} = nothing,\n    p::Union{Vector{AbstractFloat}, Nothing} = nothing,\n    model_geometry::Union{ModelGeometry, Nothing} = nothing,\n    rng::Union{AbstractRNG, Nothing} = nothing\n) where {S<:AbstractString}\n\nAdds a parameters definition to determinantal_parameters.\n\nThis method first checks that the given parameter is not already defined. If it is, this method simply flags the parameter with that name for optimization. If the given parameter is not already defined, then  it is added to the definitons in determinantal_parameters.\n\n\n\n\n\n","title":"VariationalMC.add_parameter!"},{"category":"section","location":"api.html#Jastrow-Parameters","page":"API","text":"","title":"Jastrow Parameters"},{"category":"type","location":"api.html#VariationalMC.JastrowParameters","page":"API","text":"JastrowParameters{E<:AbstractFloat}\n\nJastrow pseudopotentials for different types of Jastrow factor. These include density-density, spin-spin, phonon-phonon, and electron-phonon.\n\nFields\n\nparticle_pair::AbstractString: Pair of particles being subject to Jastrow pseudopotentials, being \"electron-electron\", \"phonon-phonon\", and \"electron-phonon\".\norder_pair::AbstractString: Pair of particle ordering types that are being reduced with Jastrow pseupotentials, being \"density-density\", \"spin-spin\", \"density-displacement\", and \"displacement-displacement\".\nirr_indices::Vector{Int}: Set of (irreducible) single indices identifying equivalence classes of lattice site index pairs. Note that irreducible indices start at 0.\nmean_v::Vector{E}: Strength of each pseudopotential for each irreducible index.\noptimize::Bool: Optimization flag for whether the pseudopotentials will be optimized. If true, all parameters will be optimized excpet for ones associated with the largest irreducible index.\norbitals::Vector{Int}: Orbital IDs for each irreducible index.\n\n\n\n\n\n","title":"VariationalMC.JastrowParameters"},{"category":"method","location":"api.html#VariationalMC.JastrowParameters-Tuple{}","page":"API","text":"JastrowParameters(;\n    particle_pair::AbstractString,\n    order_pair::AbstractString,\n    orbitals::Vector{Int},\n    optimize::Bool,\n    model_geometry::ModelGeometry,\n    rng::AbstractRNG,\n    v::Union{Vector{E}, Nothing} = nothing\n) where {E<:AbstractFloat}\n\nInitialize and return an instance of `JastrowParameters'.\n\n\n\n\n\n","title":"VariationalMC.JastrowParameters"},{"category":"section","location":"api.html#Monte-Carlo","page":"API","text":"","title":"Monte Carlo"},{"category":"page","location":"api.html","page":"API","text":"Variational Wavefunction\nMetropolis Updater\nStochastic Reconfiguration","title":"API"},{"category":"section","location":"api.html#Variational-Wavefunction","page":"API","text":"","title":"Variational Wavefunction"},{"category":"type","location":"api.html#VariationalMC.DeterminantalWavefunction","page":"API","text":"DeterminantalWavefunction{T<:Number, E<:AbstractMatrix{T}}\n\nA mutable struct containing all the parameters needed to characterize the determinantal part of the trial wavefunction.\n\nFields\n\nW::E: Equal-time Green's function matrix.\nD::E: Slater matrix.\nM::E: Reduced form of the unitary matrix U.\nA::Vector{E}: Set of logarithmic derivative operator matrices for all parameters that will be optimized. \nnq_updates_W::Int: Tracker for the number of quick updates that have been performed on the equal-time Green's function.\n\n\n\n\n\n","title":"VariationalMC.DeterminantalWavefunction"},{"category":"method","location":"api.html#VariationalMC.DeterminantalWavefunction-Tuple{}","page":"API","text":"DeterminantalWavefunction(;\n    # KEYWORD ARGUMENTS\n    determinantal_parameters::DeterminantalParameters,\n    hamiltonian::Hamiltonian{T,E},\n    particle_configuration::ParticleConfiguration,\n    model_geometry::ModelGeometry\n) where {T, E}\n\nInitialize and return an instance of DeterminantalWavefunction.\n\n\n\n\n\n","title":"VariationalMC.DeterminantalWavefunction"},{"category":"type","location":"api.html#VariationalMC.AbstractJastrowFactor","page":"API","text":"abstract type AbstractHST{T<:Number, R<:AbstractFloat} end\n\nAbstract type to represent a Jastrow factor.\n\n\n\n\n\n","title":"VariationalMC.AbstractJastrowFactor"},{"category":"type","location":"api.html#VariationalMC.FermionJastrowFactor","page":"API","text":"FermionJastrowFactor{E<:AbstractFloat} <: AbstractJastrowFactor{E}\n\nBosonJastrowFactor{E<:AbstractFloat} <: AbstractJastrowFactor{E}\n\nMixedJastrowFactor{E<:AbstractFloat} <: AbstractJastrowFactor{E}\n\nA mutable struct containing all the parameters needed to characterize a Jastrow factor involving fermions and/or bosons (phonons).\n\nFields\n\nT::Vector{Vector{E}}: Vector of Jastrow factor elements.\norbitals::Vector{Int}: Orbital IDs subject to Jastrow pseudopotentials.\nnq_updates_T::Int: Tracker for the numbner of quick updates that have been performed to vector T.\n\n\n\n\n\n","title":"VariationalMC.FermionJastrowFactor"},{"category":"method","location":"api.html#VariationalMC.JastrowFactor-Tuple{}","page":"API","text":"JastrowFactor(;\n    # KEYWORD ARGUMENTS\n    jastrow_parameters::JastrowParameters{E},\n    particle_configuration::ParticleConfiguration,\n    model_geometry::ModelGeometry\n) where {E}\n\nInitialize and return an instance of the JastrowFactor type depending upon the particle pair specified in jastrow_parameters.\n\n\n\n\n\n","title":"VariationalMC.JastrowFactor"},{"category":"section","location":"api.html#Metropolis-Updater","page":"API","text":"","title":"Metropolis Updater"},{"category":"function","location":"api.html#VariationalMC.local_fermion_update!","page":"API","text":"local_fermion_update!(;\n    # KEYWORD ARGUMENTS\n    detwf::DeterminantalWavefunction{T},\n    jastrow_factors::Union{Tuple{<:AbstractJastrowFactor{T}},Nothing} = nothing,\n    tight_binding_parameters::TightBindingParameters,\n    jastrows_parameters::Union{Tuple{JastrowParameters{T}},Nothing} = nothing,\n    particle_configuration::ParticleConfiguration,\n    model_geometry::ModelGeometry,\n    δW::AbstractFloat,\n    n_stab_W::Int,\n    rng::AbstractRNG\n) where {T}\n\nPerforms a local update to the fermionic configuration by attempting to move a particle with ID beta at a randomly  selected lattice site k to another random site l, with acceptance determined by the Metropolis-Hasting algorithm.\n\nKEYWORD ARGUMENTS\n\ndetwf::DeterminantalWavefunction{T}: Current instance of DeterminantalWavefunction.\njastrow_factors::Union{Tuple{<:AbstractJastrowFactor{T}},Nothing} = nothing: Tuple of jastrow_factor.\ntight_binding_parameters::TightBindingParameters: Instance of TightBindingParameters.\njastrows_parameters::Union{Tuple{JastrowParameters{T}},Nothing} = nothing: Tuple of jastrow_parameters.\nparticle_configuration::ParticleConfiguration: Instance of ParticleConfiguration.\nmodel_geometry::ModelGeometry: Instance of ModelGeometry.\nδW::AbstractFloat: Maximum allowed error in the equal-time Green's function matrix W.\nδT::Union{AbstractFloat,Nothing} = nothing: Maximum allowed error in the Jastrow vector T.\nn_stab_W::Int: Frequency of numerical stability checks performed on the matrix W.\nn_stab_T::Union{Int,Nothing} = nothing: Frequency of numerical stability checks performed on the vector T.\nrng::AbstractRNG: Random number generator.\n\n\n\n\n\n","title":"VariationalMC.local_fermion_update!"},{"category":"section","location":"api.html#Stochastic-Reconfiguration","page":"API","text":"","title":"Stochastic Reconfiguration"},{"category":"type","location":"api.html#VariationalMC.Optimizer","page":"API","text":"Optimizer{T<:Number}\n\nA mutable struct containing all the parameters needed to characterize a Stochastic Reconfiguration optimizer.\n\nFields\n\nvar_params::Vector{T}: All variational parameters in the model.\nη::AbstractFloat: Parameter which ensures the stability of the optimization.\ndt::AbstractFloat: Parameter which controls the rate of optimization.\nS::Matrix{T}: Covariance matrix.\nf::Vector{T}: Forces.\n\n\n\n\n\n","title":"VariationalMC.Optimizer"},{"category":"function","location":"api.html#VariationalMC.update_optimizer!","page":"API","text":"update_optimizer!(;\n    optimizer::Optimizer{T},\n    bin_size::Int,\n    determinantal_parameters::DeterminantalParameters{S,T},\n    jas_parameters::Union{Tuple{JastrowParameters{T}}, Nothing} = nothing\n) where {S, T<:Number}\n\nUpdates the optimizer by performing Stochastic Reconfiguration. The updated parameters are then pushed back to their respective containers.\n\nNOTE\n\nDuring the optimization step, this function normalizes all measurements so that the optimizer can construct S and f. Ensure that the opt field in the write_measurements! method is  set to true.\n\n\n\n\n\n","title":"VariationalMC.update_optimizer!"},{"category":"section","location":"api.html#Measurement-Methods","page":"API","text":"","title":"Measurement Methods"},{"category":"page","location":"api.html","page":"API","text":"Measurement Names\nInitialize Measurements\nMake Measurements\nWrite Measurements\nProcess Measurements\nExport Measurements","title":"API"},{"category":"section","location":"api.html#Measurement-Names","page":"API","text":"","title":"Measurement Names"},{"category":"constant","location":"api.html#VariationalMC.GLOBAL_MEASUREMENTS","page":"API","text":"GLOBAL_MEASUREMENTS = (\n    \"density\",\n    \"double_occ\",\n    \"spin-z\",\n    \"energy_per_site\"\n)\n\nList of all the global measurements that are made.\n\n\n\n\n\n","title":"VariationalMC.GLOBAL_MEASUREMENTS"},{"category":"constant","location":"api.html#VariationalMC.LOCAL_MEASUREMENTS","page":"API","text":"LOCAL_MEASUREMENTS = Base.ImmutableDict(\n    \"density\"                  => \"ORBITAL_ID\",\n    \"site-dependent_density\"   => \"ORBITAL_ID\",\n    \"double_occ\"               => \"ORBITAL_ID\",\n    \"spin-z\"                   => \"ORBITAL_ID\",\n    \"site-dependent_density\"   => \"ORBITAL_ID\"\n    \"hopping_energy\"           => \"HOPPING_ID\",\n    \"hubbard_energy\"           => \"HUBBARD_ID\",\n    \"ext_hub_energy\"           => \"EXT_HUB_ID\",\n    \"phonon_kin_energy\"        => \"PHONON_ID\",\n    \"phonon_pot_energy\"        => \"PHONON_ID\",\n    \"X\"                        => \"PHONON_ID\",\n    \"holstein_energy\"          => \"HOLSTEIN_ID\",\n    \"ssh_energy\"               => \"SSH_ID\",\n)\n\nList of all the local measurements than can be made, with a mapping to the corresponding type of ID each measurement is reported in terms of.\n\n\n\n\n\n","title":"VariationalMC.LOCAL_MEASUREMENTS"},{"category":"constant","location":"api.html#VariationalMC.CORRELATION_FUNCTIONS","page":"API","text":"CORRELATION_FUNCTIONS = Base.ImmutableDict(\n    \"density\"          => \"ORBITAL_ID\",\n    \"spin_x\"           => \"ORBITAL_ID\",\n    \"spin_z\"           => \"ORBITAL_ID\",\n    \"pair\"             => \"BOND_ID\",\n)\n\nList of all the correlation functions that can be measured, along with the corresponding type of ID the correlation measurement is reported in terms of. Correlation functions are well defined in both position and momentum space.\n\n\n\n\n\n","title":"VariationalMC.CORRELATION_FUNCTIONS"},{"category":"section","location":"api.html#Initialize-Measurements","page":"API","text":"","title":"Initialize Measurements"},{"category":"function","location":"api.html#VariationalMC.initialize_measurement_container","page":"API","text":"initialize_measurement_container(\n    determinantal_parameters::DeterminantalParameters,\n    model_geometry::ModelGeometry{D,T,N},\n    N_opt::Int,\n    N_sim::Int,\n    opt_bin_size::Int,\n    sim_bin_size::Int;\n    jas_parameters::Union{Tuple{JastrowParameters},Nothing}=nothing\n) where {T<:AbstractFloat, D, N}\n\nInitialize and return a measurement container of type NamedTuple.\n\n\n\n\n\n","title":"VariationalMC.initialize_measurement_container"},{"category":"function","location":"api.html#VariationalMC.initialize_measurements!","page":"API","text":"initialize_measurements!(\n    measurement_container::NamedTuple,\n    hubbard_model::HubbardModel{T}\n) where {T<:AbstractFloat}\n\nInitialize Hubbard model related measurements.\n\nInitialized Measurements\n\nhubbard_energy: Refer to measure_hopping_energy.\n\n\n\n\n\ninitialize_measurements!(\n    measurement_container::NamedTuple,\n    extended_hubbard_model::ExtendedHubbardModel{T}\n) where {T<:AbstractFloat}\n\nInitialize Extended Hubbard model related measurements.\n\nInitialized Measurements\n\next_hubbard_energy: Refer to measure_ext_hubbard_energy.\n\n\n\n\n\n","title":"VariationalMC.initialize_measurements!"},{"category":"function","location":"api.html#VariationalMC.initialize_correlation_measurements!","page":"API","text":"initialize_correlation_measurements!(;\n    measurement_container::NamedTuple,\n    correlation::String,\n    pairs::AbstractVector{NTuple{2,Int}},\n    time_displaced::Bool,\n    integrated::Bool = false\n)  where {T<:AbstractFloat, D, N}\n\nInitialize measurements of correlation for all ID pairs; refer to CORRELATION_FUNCTIONS for ID type associated with each correlation measurement. The name correlation must therefore also appear in [CORRELATION_FUNCTIONS]@ref.\n\n\n\n\n\n","title":"VariationalMC.initialize_correlation_measurements!"},{"category":"section","location":"api.html#Make-Measurements","page":"API","text":"","title":"Make Measurements"},{"category":"function","location":"api.html#VariationalMC.make_measurements!","page":"API","text":"make_measurements!(\n    measurement_container::NamedTuple;\n    detwf::DeterminantalWavefunction{T},\n    jas_factors::Union{Tuple{<:AbstractJastrowFactor{T}}, Nothing} = nothing,\n    tight_binding_parameters::TightBindingParameters{T, E},\n    jas_parameters::Union{Tuple{JastrowParameters{E}}, Nothing} = nothing,\n    coupling_parameters::Tuple,\n    particle_configuration::ParticleConfiguration,\n    model_geometry::ModelGeometry;\n    opt_step::Bool = false\n) where {T<:Number, E<:AbstractFloat}\n\nMake measurements. If used during the optimization phase of the simulation, set opt_step = true, which will activate the  measurements of the logarithmic derivatives.\n\n\n\n\n\n","title":"VariationalMC.make_measurements!"},{"category":"section","location":"api.html#Write-Measurements","page":"API","text":"","title":"Write Measurements"},{"category":"function","location":"api.html#VariationalMC.write_measurements!","page":"API","text":"write_measurements!(;\n    # KEYWORD ARGUMENTS\n    measurement_container::NamedTuple,\n    simulation_info::SimulationInfo,\n    model_geometry::ModelGeometry,\n    bin::Int,\n    bin_size::Int,\n    opt_step::Bool = false\n)\n\nWrite the measurements contained in measurement_container to file. Measurements are written to file in HDF5 format.\n\n\n\n\n\n","title":"VariationalMC.write_measurements!"},{"category":"function","location":"api.html#VariationalMC.merge_bins","page":"API","text":"merge_bins(\n    simulation_info::SimulationInfo;\n    opt::Bool = false\n)\n\nMerge the separate HDF5 files containing the binned measurements into a single HDF5 file. This is true even if the HDF5 \"files\" containing the binned data were held in memory during the simulation (simulation_info.write_bins_concurrent = false) instead of being actively written to file during the simulation (simulation_info.write_bins_concurrent = true). Set opt = true if merging binds from the optimization step.\n\n\n\n\n\n","title":"VariationalMC.merge_bins"},{"category":"function","location":"api.html#VariationalMC.rm_bins","page":"API","text":"rm_bins(\n    comm::MPI.Comm,\n    datafolder::String\n)\n\nrm_bins(\n    datafolder::String\n)\n\nDelete the binned data stored in the datafolder directory. This function essentially deletes the directory datafolder/bins and its contents.\n\n\n\n\n\n","title":"VariationalMC.rm_bins"},{"category":"section","location":"api.html#Process-Measurements","page":"API","text":"","title":"Process Measurements"},{"category":"function","location":"api.html#VariationalMC.save_simulation_info","page":"API","text":"save_simulation_info(\n    # ARGUMENTS\n    sim_info::SimulationInfo,\n    metadata = nothing;\n    # KEYWORD ARGUMENTS\n    filename = @sprintf \"simulation_info_sID-%d_pID-%d.toml\" sim_info.sID sim_info.pID\n)\n\nSave the contents sim_info to a TOML file, and add an optional additional table to the output file based on the contents of a dictionary metadata.\n\n\n\n\n\n","title":"VariationalMC.save_simulation_info"},{"category":"function","location":"api.html#VariationalMC.process_measurements","page":"API","text":"process_measurements(\n    # ARGUMENTS\n    comm::MPI.Comm;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    ...\n)\n\nprocess_measurements(;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    ...\n)\n\nProcess the HDF5 files containing the binned data generated by a DQMC simulation, and then write final statistics to a new HDF5 file(s). There is also functionality available that allows the final stats to then be exported to CSV files as well.\n\nWhen the first passed arguments is comm::MPI.Comm, then the rebinning of data can be parallelized using MPI. However, it will only be parallelized if parallelize_rebinning = true.\n\nKeyword Arguments\n\nThis function has many possible keyword arguments. Below I group them together into related categories and define their meanings.\n\nRequired Keyword Arguments\n\ndatafolder::String: Specify the directory generated by a DQMC simulation into which all the results were written.\n\nKeyword for Controlling Workflow\n\nparallelize_rebinning::Bool = true: If comm::MPI.Comm is passed as first argument to this function,  then if this is true then rebinning is done on each MPI process and then collected together on the root process  where jackknife is used to calculate mean and error of observables.  Otherwise all the rebinning is performed by the root process alone.\nn_bins::Union{Int, Nothing} = nothing: Number of bins used per walker to calculate statistics.  If nothing then set equal to the number of data bins written to file during the simulation.  Must be a factor of the number of bins written to file per walker during the DQMC simulation.\npIDs::Union{Int,Vector{Int}} = Int[]: Specifies for which process IDs to calculate average statistics.  If pIDs = Int[], the calculate for all process IDs.  If comm::MPI.Comm is passed as first function argument then pIDs must be of type Vector{Int} and not Int.\nfilename_prefix::String = \"stats\": Start of filename for HDF5 containing final statistics.  HDF5 files containing statistics for a single process ID will end with pID-$(pID).h5.\nrm_binned_data::Bool = false: Whether to delete the binned data after final statistics are computed.\n\nKeywords for Exporting Statistics to CSV\n\nexport_to_csv::Bool = true: Whether to export the final statistics to CSV file.\nscientific_notation::Bool = false: Whether to use scientific notation when exporting statistics to CSV file.\ndecimals::Int = 9: How many decimal places to include when exporting statistics to CSV files.\ndelimiter::String = \" \": Delimiter used when writing CSV files.\n\nKeyword Acting as Boolean Flags Indicating Which Statistics to Compute\n\nprocess_global_measurements::Bool = true: Whether to calculate the statistics for global measurements.\nprocess_local_measurements::Bool = true: Whether to calculate the statistics for local measurements.\nprocess_all_equal_time_measurements::Bool = true: Whether to calculate statistics for all equal-time correlation measurements.\n\n\n\n\n\n","title":"VariationalMC.process_measurements"},{"category":"section","location":"api.html#Export-Measurements","page":"API","text":"","title":"Export Measurements"},{"category":"function","location":"api.html#VariationalMC.export_global_stats_to_csv","page":"API","text":"export_global_stats_to_csv(\n    # ARGUMENTS\n    comm::MPI.Comm;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    h5filename::String = \"stats.h5\",,\n    csvfilename_prefix::String = \"global\",\n    measurements::Vector{String} = String[],\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nexport_global_stats_to_csv(;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    h5filename::String = \"stats.h5\",,\n    csvfilename_prefix::String = \"global\",\n    measurements::Vector{String} = String[],\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nThis function writes the global measurement statistics stored in the h5filename HDF5 file found in the directory datafolder to CSV file, returning the name of the CSV file that was written. The measurements keyword argument specifies the measurements to be exported. If measurements = String[], then all measurements are exported.\n\n\n\n\n\n","title":"VariationalMC.export_global_stats_to_csv"},{"category":"function","location":"api.html#VariationalMC.export_local_stats_to_csv","page":"API","text":"export_local_stats_to_csv(\n    # ARGUMENTS\n    comm::MPI.Comm;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    h5filename::String = \"stats.h5\",\n    csvfilename_prefix::String = \"local\",\n    measurements::Vector{String} = String[],\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nexport_local_stats_to_csv(;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    h5filename::String = \"stats.h5\",\n    csvfilename_prefix::String = \"local\",\n    measurements::Vector{String} = String[],\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nThis function writes the local measurement statistics stored in the h5filename HDF5 file found in the directory datafolder to CSV file, returning the name of the CSV file that was written. The measurements keyword argument specifies the measurements to be exported. If measurements = String[], then all measurements are exported.\n\n\n\n\n\n","title":"VariationalMC.export_local_stats_to_csv"},{"category":"function","location":"api.html#VariationalMC.export_correlation_stats_to_csv","page":"API","text":"export_correlation_stats_to_csv(\n    # ARGUMENTS\n    comm::MPI.Comm;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    correlation::String,\n    type::String,\n    space::String,\n    h5filename::HDF5.File = \"stats.h5\",\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nexport_correlation_stats_to_csv(;\n    # KEYWORD ARGUMENTS\n    datafolder::String,\n    correlation::String,\n    type::String,\n    space::String,\n    h5filename::HDF5.File = \"stats.h5\",\n    decimals::Int = 6,\n    scientific_notation::Bool = false,\n    delimiter::String = \" \"\n)\n\nExport statistics for specified type of correlation function from HDF5 file to a CSV file.\n\n\n\n\n\n","title":"VariationalMC.export_correlation_stats_to_csv"},{"category":"section","location":"examples/hubbard_chain.html#Hubbard-Chain","page":"Hubbard Chain","text":"","title":"Hubbard Chain"},{"category":"page","location":"examples/hubbard_chain.html","page":"Hubbard Chain","text":"Download this example as a Julia script.","title":"Hubbard Chain"},{"category":"page","location":"examples/hubbard_chain.html","page":"Hubbard Chain","text":"This is a ready-to-run script for simulating a one-dimensional (1D) Hubbard model with a Néel antiferromagnetic wavefunction.","title":"Hubbard Chain"},{"category":"page","location":"examples/hubbard_chain.html","page":"Hubbard Chain","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\n\n# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    nup,                    # Number of spin-up Fermions.\n    ndn,                    # Number of spin-down Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_chain_nup%d_ndn%d_U%.2f_L%d\" nup ndn U L\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0]],\n        basis_vecs   = [[0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [L],\n        [true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    add_bond!(model_geometry, bond_x)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        nup = nup, ndn = ndn,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,           # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x],      # defines hopping.\n        t_mean  = [t]            # defines corresponding mean hopping amplitude.\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )\n\n    # Initialize tight-binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard interaction parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add the spin-z variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"spin-z\",\n        order_type      = \"spin\",\n        symmetry        = \"uniform\"\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),\n        L               = parse(Int,     ARGS[2]),\n        U               = parse(Float64, ARGS[3]),\n        nup             = parse(Int,     ARGS[4]),\n        ndn             = parse(Int,     ARGS[5]),\n        ph_transform    = parse(Bool,    ARGS[6]),\n        N_equil         = parse(Int,     ARGS[7]),\n        N_opt           = parse(Int,     ARGS[8]),\n        N_sim           = parse(Int,     ARGS[9]),\n        N_opt_bins      = parse(Int,     ARGS[10]),\n        N_sim_bins      = parse(Int,     ARGS[11])\n    )\nend","title":"Hubbard Chain"},{"category":"section","location":"examples/hubbard_ladder.html#Hubbard-Ladder","page":"Hubbard Ladder","text":"","title":"Hubbard Ladder"},{"category":"page","location":"examples/hubbard_ladder.html","page":"Hubbard Ladder","text":"Download this example as a Julia script.","title":"Hubbard Ladder"},{"category":"page","location":"examples/hubbard_ladder.html","page":"Hubbard Ladder","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a rectangular lattice goemetry (ladder) with a stripe wavefunction.","title":"Hubbard Ladder"},{"category":"page","location":"examples/hubbard_ladder.html","page":"Hubbard Ladder","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\n\n# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    Lx,                     # System size in the x-direction.\n    Ly,                     # System size in the y-direction.\n    t′,                     # Next-nearest neighbor hopping amplitude.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_tp%.2f_U%.2f_Lx%d_Ly%d\" density t′ U Lx Ly\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [Lx, Ly],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Define the positive next nearest-neighbor bond.\n    bond_xy = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,1]\n    )\n    bond_xy_id = add_bond!(model_geometry, bond_xy)\n\n    # Define the negative next nearest-neighbor bond.\n    bond_yx = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,-1]\n    )\n    bond_yx_id = add_bond!(model_geometry, bond_yx)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                                      # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y, bond_xy, bond_yx],       # defines hopping.\n        t_mean  = [t, t, t′, t′]                            # defines corresponding mean hopping amplitude.\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )\n\n    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add charge stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"density\",\n        order_type  = \"charge\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1],\n        p           = [0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.3, 0.3, 0.5, 0.5, 0.5, 0.5, 0.5, 0.5, 0.3, 0.3],\n        model_geometry = model_geometry\n    )\n\n    # Add spin stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"spin-z\",\n        order_type  = \"spin\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1],\n        p           = [0.5, -0.5, 0.5, -0.5, 0.5, -0.5, 0.0, 0.0, -0.5, 0.5, -0.5, 0.5, -0.5, 0.5, 0.0, 0.0],\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"spin-spin\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize site-dependent density measurements.\n    initialize_site_dependent_measurements!(\n        measurement_container   = measurement_container,\n        model_geometry          = model_geometry,\n        observable              = \"density\"\n    )\n\n    # Initialize site-dependent spin measurements.\n    initialize_site_dependent_measurements!(\n        measurement_container   = measurement_container,\n        model_geometry          = model_geometry,\n        observable              = \"spin-z\"\n    )\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),\n        L               = parse(Int,     ARGS[2]),\n        t′              = parse(Float64, ARGS[3]),\n        U               = parse(Float64, ARGS[4]),\n        density         = parse(Float64, ARGS[5]),\n        ph_transform    = parse(Bool,    ARGS[6]),\n        N_equil         = parse(Int,     ARGS[7]),\n        N_opt           = parse(Int,     ARGS[8]),\n        N_sim           = parse(Int,     ARGS[9]),\n        N_opt_bins      = parse(Int,     ARGS[10]),\n        N_sim_bins      = parse(Int,     ARGS[11])\n    )\nend","title":"Hubbard Ladder"},{"category":"section","location":"examples/hubbard_ladders_mpi.html#Interacting-Hubbard-Ladders-with-MPI-Parallelization","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladders_mpi.html","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"Download this example as a Julia script.","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladders_mpi.html","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a rectangular lattice goemetry (ladders) with a stripe wavefunction using MPI parallelization.","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladders_mpi.html","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\nusing MPI\n\n# Top-level function to run the simulation\nfunction run_simulation(\n    comm::MPI.Comm;         # MPI communicator.\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    Lx,                     # System size in the x-direction.\n    Ly,                     # System size in the y-direction.\n    t₁′,                    # Next-nearest neighbor hopping amplitude in ladder 1.\n    t₂′,                    # Next-nearest neighbor hopping amplitude in ladder 2.\n    U,                      # Hubbard interaction.\n    V,                      # Extended Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_ladder_n%.3f_U%.2f_Lx%d_Ly%d\" density U Lx Ly\n\n    # Get MPI process ID.\n    pID = MPI.Comm_rank(comm)\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID,\n        pID = pID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladders_mpi.html","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"#Initialize the metadata dictionary.","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"page","location":"examples/hubbard_ladders_mpi.html","page":"Interacting Hubbard Ladders with MPI Parallelization","text":"    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0], [1.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions in the x-direction.\n    lattice = lu.Lattice(\n        [Lx, Ly],\n        [true, false]       # TODO: check that doing this is OK\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor x-bond in ladder 1.\n    bond_x1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n    bond_x1_id = add_bond!(model_geometry, bond_x1)\n\n    # Define the nearest-neighbor y-bond in ladder 1.\n    bond_y1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n    bond_y1_id = add_bond!(model_geometry, bond_y1)\n\n    # Define the postive next-nearest neighbor in ladder 1.\n    bond_xy1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,1]\n    )\n    bond_xy1_id = add_bond!(model_geometry, bond_xy1)\n\n    # Define the negative next-nearest neighbor in ladder 1.\n    bond_yx1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,-1]\n    )\n    bond_yx1_id = add_bond!(model_geometry, bond_yx1)\n\n    # Define the nearest-neighbor x-bond in ladder 2.\n    bond_x2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,0]\n    )\n    bond_x2_id = add_bond!(model_geometry, bond_x2)\n\n    # Define the nearest-neighbor y-bond in ladder 2.\n    bond_y2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [0,1]\n    )\n    bond_y2_id = add_bond!(model_geometry, bond_y2)\n\n    # Define the postive next-nearest neighbor in ladder 2.\n    bond_xy2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,1]\n    )\n    bond_xy2_id = add_bond!(model_geometry, bond_xy2)\n\n    # Define the negative next-nearest neighbor in ladder 2.\n    bond_yx2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,-1]\n    )\n    bond_yx2_id = add_bond!(model_geometry, bond_yx2)\n\n    # Define a nearest-neighbor interaction bond between the ladders 1 and 2.\n    bond_int1 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [-1,-1]\n    )\n    bond_int1_id = add_bond!(model_geometry, bond_int1)\n\n    # Define a nearest-neighbor interaction bond between the ladders 1 and 2.\n    bond_int2 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [0,-1]\n    )\n    bond_int2_id = add_bond!(model_geometry, bond_int2)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Nearest-neighbor hopping amplitude for the first ladder.\n    t₁ = 1.0\n\n    # Nearest-neighbor hopping ampplitude for the second ladder.\n    t₂ = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0., # set initial estimate for the chemical potential\n        t_bonds = [bond_x1, bond_y1, bond_x2, bond_y2, bond_xy1, bond_yx1, bond_xy2, bond_yx2], # defines hopping\n        t_mean  = [t₁, t₁, t₂, t₂, t₁′, t₁′, t₂′, t₂′] # defines corresponding mean hopping amplitude\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1, 2],\n        U_mean      = [U, U]\n    )\n\n    # Define the Hubbard interaction between the ladders.\n    extended_hubbard_model = ExtendedHubbardModel(\n        model_geometry = model_geometry,\n        V_bond = [bond_int1, bond_int2],\n        V_mean = [V, V]\n    )\n\n    # Initialize tight-binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard interaction parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize extended Hubbard interaction parameters.\n    extended_hubbard_parameters = ExtendedHubbardParameters(\n        extended_hubbard_model = extended_hubbard_model,\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add charge stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"density\",\n        order_type  = \"charge\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Add spin stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"spin-z\",\n        order_type  = \"spin\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"spin-spin\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model, extended_hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the extended Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, extended_hubbard_model)\n\n    # Initialize equal-time charge correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"density\",\n        pairs = [(1,2)]\n    )\n\n    # Initialize equal-time spin correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"spin-z\",\n        pairs = [(1,2)]\n    )\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system.\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Initialize MPI\n    MPI.Init()\n\n    # Initialize the MPI communicator.\n    comm = MPI.COMM_WORLD\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(\n        comm;\n        sID             = parse(Int,     ARGS[1]),\n        Lx              = parse(Int,     ARGS[2]),\n        Ly              = parse(Int,     ARGS[3]),\n        t₁′             = parse(Float64, ARGS[4]),\n        t₂′             = parse(Float64, ARGS[5]),\n        U               = parse(Float64, ARGS[6]),\n        V               = parse(Float64, ARGS[7]),\n        density         = parse(Float64, ARGS[8]),\n        ph_transform    = parse(Bool,    ARGS[9]),\n        N_equil         = parse(Int,     ARGS[10]),\n        N_opt           = parse(Int,     ARGS[11]),\n        N_sim           = parse(Int,     ARGS[12]),\n        N_opt_bins      = parse(Int,     ARGS[13]),\n        N_sim_bins      = parse(Int,     ARGS[14])\n    )\n\n    # Finalize MPI.\n    MPI.Finalize()\nend","title":"Interacting Hubbard Ladders with MPI Parallelization"},{"category":"section","location":"examples/hubbard_ladders.html#Interacting-Hubbard-Ladders","page":"Interacting Hubbard Ladders","text":"","title":"Interacting Hubbard Ladders"},{"category":"page","location":"examples/hubbard_ladders.html","page":"Interacting Hubbard Ladders","text":"Download this example as a Julia script.","title":"Interacting Hubbard Ladders"},{"category":"page","location":"examples/hubbard_ladders.html","page":"Interacting Hubbard Ladders","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a rectangular lattice goemetry (ladders) with a stripe wavefunction.","title":"Interacting Hubbard Ladders"},{"category":"page","location":"examples/hubbard_ladders.html","page":"Interacting Hubbard Ladders","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\n\n# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    Lx,                     # System size in the x-direction.\n    Ly,                     # System size in the y-direction.\n    t₁′,                    # Next-nearest neighbor hopping amplitude in ladder 1.\n    t₂′,                    # Next-nearest neighbor hopping amplitude in ladder 2.\n    U,                      # Hubbard interaction.\n    V,                      # Extended Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_ladder_n%.3f_tp1%.2f_tp2%.2f_U%.2f_V%.2f_Lx%d_Ly%d\" density t₁′ t₂′ U V Lx Ly\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0], [1.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions in the x-direction.\n    lattice = lu.Lattice(\n        [Lx, Ly],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor x-bond in ladder 1.\n    bond_x1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n    bond_x1_id = add_bond!(model_geometry, bond_x1)\n\n    # Define the nearest-neighbor y-bond in ladder 1.\n    bond_y1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n    bond_y1_id = add_bond!(model_geometry, bond_y1)\n\n    # Define the postive next-nearest neighbor in ladder 1.\n    bond_xy1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,1]\n    )\n    bond_xy1_id = add_bond!(model_geometry, bond_xy1)\n\n    # Define the negative next-nearest neighbor in ladder 1.\n    bond_yx1 = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,-1]\n    )\n    bond_yx1_id = add_bond!(model_geometry, bond_yx1)\n\n    # Define the nearest-neighbor x-bond in ladder 2.\n    bond_x2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,0]\n    )\n    bond_x2_id = add_bond!(model_geometry, bond_x2)\n\n    # Define the nearest-neighbor y-bond in ladder 2.\n    bond_y2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [0,1]\n    )\n    bond_y2_id = add_bond!(model_geometry, bond_y2)\n\n    # Define the postive next-nearest neighbor in ladder 2.\n    bond_xy2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,1]\n    )\n    bond_xy2_id = add_bond!(model_geometry, bond_xy2)\n\n    # Define the negative next-nearest neighbor in ladder 2.\n    bond_yx2 = lu.Bond(\n        orbitals = (2,2),\n        displacement = [1,-1]\n    )\n    bond_yx2_id = add_bond!(model_geometry, bond_yx2)\n\n    # Define a nearest-neighbor interaction bond between the ladders 1 and 2.\n    bond_int1 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [-1,-1]\n    )\n    bond_int1_id = add_bond!(model_geometry, bond_int1)\n\n    # Define a nearest-neighbor interaction bond between the ladders 1 and 2.\n    bond_int2 = lu.Bond(\n        orbitals = (1,2),\n        displacement = [0,-1]\n    )\n    bond_int2_id = add_bond!(model_geometry, bond_int2)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Nearest-neighbor hopping amplitude for the first ladder.\n    t₁ = 1.0\n\n    # Nearest-neighbor hopping ampplitude for the second ladder.\n    t₂ = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0., # set initial estimate for the chemical potential\n        t_bonds = [bond_x1, bond_y1, bond_x2, bond_y2, bond_xy1, bond_yx1, bond_xy2, bond_yx2], # defines hopping\n        t_mean  = [t₁, t₁, t₂, t₂, t₁′, t₁′, t₂′, t₂′] # defines corresponding mean hopping amplitude\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1, 2],\n        U_mean      = [U, U]\n    )\n\n    # Define the Hubbard interaction between the ladders.\n    extended_hubbard_model = ExtendedHubbardModel(\n        model_geometry = model_geometry,\n        V_bond = [bond_int1, bond_int2],\n        V_mean = [V, V]\n    )\n\n    # Initialize tight-binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard interaction parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize extended Hubbard interaction parameters.\n    extended_hubbard_parameters = ExtendedHubbardParameters(\n        extended_hubbard_model = extended_hubbard_model,\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add charge stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"density\",\n        order_type  = \"charge\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Add spin stripe parameters for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name  = \"spin-z\",\n        order_type  = \"spin\",\n        symmetry    = \"site-dependent\",\n        optimize    = true,\n        orbital     = [1, 2],\n        model_geometry = model_geometry\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"spin-spin\",\n        orbitals        = [1, 2],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model, extended_hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the extended Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, extended_hubbard_model)\n\n    # Initialize equal-time charge correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"density\",\n        pairs = [(1,2)]\n    )\n\n    # Initialize equal-time spin correlation measurements.\n    initialize_correlation_measurements!(\n        measurement_container = measurement_container,\n        model_geometry = model_geometry,\n        correlation = \"spin-z\",\n        pairs = [(1,2)]\n    )\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters, extended_hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \",\n        process_all_equal_time_measurements = true\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),\n        Lx              = parse(Int,     ARGS[2]),\n        Ly              = parse(Int,     ARGS[3]),\n        t₁′             = parse(Float64, ARGS[4]),\n        t₂′             = parse(Float64, ARGS[5]),\n        U               = parse(Float64, ARGS[6]),\n        V               = parse(Float64, ARGS[7]),\n        density         = parse(Float64, ARGS[8]),\n        ph_transform    = parse(Bool,    ARGS[9]),\n        N_equil         = parse(Int,     ARGS[10]),\n        N_opt           = parse(Int,     ARGS[11]),\n        N_sim           = parse(Int,     ARGS[12]),\n        N_opt_bins      = parse(Int,     ARGS[13]),\n        N_sim_bins      = parse(Int,     ARGS[14])\n    )\nend","title":"Interacting Hubbard Ladders"},{"category":"section","location":"examples/hubbard_square.html#Hubbard-Square","page":"Hubbard Square","text":"","title":"Hubbard Square"},{"category":"page","location":"examples/hubbard_square.html","page":"Hubbard Square","text":"Download this example as a Julia script.","title":"Hubbard Square"},{"category":"page","location":"examples/hubbard_square.html","page":"Hubbard Square","text":"This is a ready-to-run script for simulating a two-dimensional (2D) Hubbard model on a square lattice with an onsite s-wave pairing wavefunction.","title":"Hubbard Square"},{"category":"page","location":"examples/hubbard_square.html","page":"Hubbard Square","text":"# Import the required packages\nusing VariationalMC\nimport VariationalMC.LatticeUtilities as lu\n\nusing Random\nusing Printf\n\n# Top-level function to run the simulation\nfunction run_simulation(;\n    # KEYWORD ARGUMENTS\n    sID,                    # Simulation ID.\n    L,                      # System size.\n    U,                      # Hubbard interaction.\n    density,                # Density of Fermions.\n    ph_transform,           # Whether the model is particle-hole transformed.\n    N_equil,                # Number of equilibration/thermalization updates.\n    N_opt,                  # Number of optimization steps.\n    N_sim,                  # Number of simulation steps.\n    N_opt_bins,             # Number of optimization bins.\n    N_sim_bins,             # Number of simulation bins.\n    dt = 0.1,               # Optimization rate.\n    η = 1e-4,               # Optimization stablity factor.\n    n_stab_W = 50,          # Green's function stabilization frequency.\n    δW = 1e-3,              # Maximum allowed error in the Green's function.\n    n_stab_T = 50,          # Jastrow factor stabilization frequency.\n    δT = 1e-3,              # Maximum allowed error in the Jastrow factor.\n    seed = abs(rand(Int)),  # Seed for random number generator.\n    filepath=\".\"            # Filepath to where data folder will be created.\n)\n    # Construct the foldername the data will be written to.\n    datafolder_prefix = @sprintf \"hubbard_square_n%.3f_U%.2f_L%d\" density U L\n\n    # Initialize simulation info.\n    simulation_info = SimulationInfo(\n        filepath = filepath,\n        datafolder_prefix = datafolder_prefix,\n        sID = sID\n    )\n\n    # Initialize the directory the data will be written to.\n    initialize_datafolder(simulation_info)\n\n    # Initialize a random number generator.\n    rng = Xoshiro(seed)\n\n    # Initialize the metadata dictionary.\n    metadata = Dict()\n\n    # Record simulation parameters.\n    metadata[\"N_equil\"] = N_equil\n    metadata[\"N_opt\"] = N_opt\n    metadata[\"N_sim\"] = N_sim\n    metadata[\"N_opt_bins\"] = N_opt_bins\n    metadata[\"N_sim_bins\"] = N_sim_bins\n    metadata[\"δW\"] = δW\n    metadata[\"δT\"] = δT\n    metadata[\"n_stab_W\"] = n_stab_W\n    metadata[\"n_stab_T\"] = n_stab_T\n    metadata[\"dt\"] = dt\n    metadata[\"acceptance_rate\"] = 0.0\n    metadata[\"seed\"] = seed\n\n    # Define the unit cell.\n    unit_cell = lu.UnitCell(\n        lattice_vecs = [[1.0, 0.0], [0.0, 1.0]],\n        basis_vecs   = [[0.0, 0.0]]\n    )\n\n    # Define a finite lattice with periodic boundary conditions.\n    lattice = lu.Lattice(\n        [L, L],\n        [true, true]\n    )\n\n    # Initialize model geometry.\n    model_geometry = ModelGeometry(\n        unit_cell, lattice\n    )\n\n    # Define the nearest-neighbor bond in the x-direction.\n    bond_x = lu.Bond(\n        orbitals = (1,1),\n        displacement = [1,0]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_x_id = add_bond!(model_geometry, bond_x)\n\n    # Define the nearest-neighbor bond in the y-direction.\n    bond_y = lu.Bond(\n        orbitals = (1,1),\n        displacement = [0,1]\n    )\n\n    # Add this bond definition to the model by adding it to the `model_geometry`.\n    bond_y_id = add_bond!(model_geometry, bond_y)\n\n    # Determine the density and initialize the configuration of Fermions.\n    particle_configuration = ParticleConfiguration(\n        density,\n        model_geometry = model_geometry,\n        ph_transform = ph_transform\n    )\n\n    # Set nearest-neighbor hopping amplitude to unity,\n    # setting the energy scale in the model.\n    t = 1.0\n\n    # Define the non-interacting tight binding model.\n    tight_binding_model = TightBindingModel(\n        model_geometry = model_geometry,\n        μ       =  0.,                    # set an initial estimate for the chemical potential.\n        t_bonds = [bond_x, bond_y],       # defines hopping.\n        t_mean  = [t, t]                  # defines corresponding mean hopping amplitude.\n    )\n\n    # Define the Hubbard interaction in the model.\n    hubbard_model = HubbardModel(\n        U_orbital   = [1],\n        U_mean      = [U]\n    )\n\n    # Initialize tight binding parameters.\n    tight_binding_parameters = TightBindingParameters(\n        tight_binding_model     = tight_binding_model,\n        particle_configuration  = particle_configuration,\n        model_geometry          = model_geometry\n    )\n\n    # Initialize Hubbard parameters.\n    hubbard_parameters = HubbardParameters(\n        hubbard_model   = hubbard_model,\n        model_geometry  = model_geometry\n    )\n\n    # Initialize variational parameters in the determinantal-part of the wavefunction.\n    determinantal_parameters = DeterminantalParameters(\n        tight_binding_parameters    = tight_binding_parameters,\n        model_geometry              = model_geometry,\n        rng                         = rng\n    )\n\n    # Add an onsite s-wave variational parameter for optimization.\n    add_parameter!(\n        determinantal_parameters,\n        param_name      = \"s-wave\",\n        order_type      = \"pair\",\n        symmetry        = \"uniform\",\n        optimize        = true,\n        orbital         = [1]\n    )\n\n    # Initialize variational parameters in the form of Jastrow pseudopotentials.\n    jastrow_parameters = JastrowParameters(\n        particle_pair   = \"electron-electron\",\n        order_pair      = \"density-density\",\n        orbitals        = [1],\n        optimize        = true,\n        model_geometry  = model_geometry,\n        rng             = rng\n    )\n\n    # Write model summary TOML file specifying Hamiltonian that will be simulated.\n    model_summary(\n        simulation_info         = simulation_info,\n        model_geometry          = model_geometry,\n        particle_configuration  = particle_configuration,\n        tight_binding_model     = tight_binding_model,\n        interactions            = (hubbard_model,),\n        parameters              = (determinantal_parameters, jastrow_parameters,)\n    )\n\n    # Initialize the container where measurements will be accumulated.\n    measurement_container = initialize_measurement_container(\n        determinantal_parameters,\n        model_geometry,\n        (jastrow_parameters,)\n    )\n\n    # Initialize the tight-binding model related measurements, namely the hopping energy.\n    initialize_measurements!(measurement_container, tight_binding_model)\n\n    # Initialize the Hubbard interaction related measurements.\n    initialize_measurements!(measurement_container, hubbard_model)\n\n    # Initialize the optimzer that will be used for Stochastic Reconfiguration.\n    optimizer = Optimizer(\n        determinantal_parameters = determinantal_parameters,\n        η = η, dt = dt,\n        jas_parameters = (jastrow_parameters,)\n    )\n\n    # Calculate the size of each optimization bin.\n    opt_bin_size = div(N_opt, N_opt_bins)\n\n    # Iterate over the optimization bins.\n    for bin in 1:N_opt_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry, opt_step = true\n            )\n        end\n\n        # Attempt to update the variational parameters.\n        update_optimizer!(\n            optimizer = optimizer,\n            measurement_container = measurement_container,\n            bin_size = opt_bin_size,\n            determinantal_parameters = determinantal_parameters,\n            jas_parameters = (jastrow_parameters,)\n        )\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = opt_bin_size,\n            opt_step = true\n        )\n    end\n\n    # Calculate the size of each simulation bin.\n    sim_bin_size = div(N_sim, N_sim_bins)\n\n    # Iterate over the simulation bins.\n    for bin in 1:N_sim_bins\n\n        # Construct the auxiliary/mean-field Hamiltonian.\n        hamiltonian = Hamiltonian(\n            tight_binding_parameters    = tight_binding_parameters,\n            determinantal_parameters    = determinantal_parameters,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry\n        )\n\n        # Construct the determinantal/Slater wavefunction.\n        detwf = DeterminantalWavefunction(\n            determinantal_parameters    = determinantal_parameters,\n            hamiltonian                 = hamiltonian,\n            particle_configuration      = particle_configuration,\n            model_geometry              = model_geometry,\n            rng                         = rng\n        )\n\n        # Construct the Jastrow factor.\n        jfac = JastrowFactor(\n            jastrow_parameters      = jastrow_parameters,\n            particle_configuration  = particle_configuration,\n            model_geometry          = model_geometry\n        )\n\n        # Iterate over the length of the bin.\n        for n in 1:opt_bin_size\n            # Equilibrate/thermalize the system\n            for equil in 1:N_equil\n                # Attempt to update the fermion configuration.\n                accepted = local_fermion_update!(\n                    detwf = detwf, jas_factors = (jfac,),\n                    tight_binding_parameters = tight_binding_parameters, jas_parameters = (jastrow_parameters,),\n                    particle_configuration = particle_configuration,\n                    model_geometry = model_geometry,\n                    δW = δW, δT = δT,\n                    n_stab_W = n_stab_W, n_stab_T = n_stab_T, rng = rng\n                )\n\n                # Record the acceptance rate.\n                metadata[\"acceptance_rate\"] += accepted\n            end\n\n            # Make measurements.\n            make_measurements!(\n                measurement_container,\n                detwf = detwf, jas_factors = (jfac,),\n                tight_binding_parameters = tight_binding_parameters,\n                determinantal_parameters = determinantal_parameters,\n                jas_parameters = (jastrow_parameters,),\n                coupling_parameters = (hubbard_parameters,),\n                particle_configuration = particle_configuration,\n                model_geometry = model_geometry\n            )\n        end\n\n        # Write the measurements.\n        write_measurements!(\n            measurement_container = measurement_container,\n            simulation_info = simulation_info,\n            model_geometry = model_geometry,\n            bin = bin,\n            bin_size = sim_bin_size\n        )\n    end\n\n    # Merge binned optimization data into a single HDF5 file.\n    merge_bins(simulation_info, opt = true)\n\n    # Merge binned simulation data into a single HDF5 file.\n    merge_bins(simulation_info)\n\n    # Normalize the acceptance rate.\n    metadata[\"acceptance_rate\"] /= N_equil * (N_opt + N_sim)\n\n    # Write the simulation summary TOML file.\n    save_simulation_info(simulation_info, metadata)\n\n    # Process the simulation results, calculating final error bars for all measurements.\n    # writing final statistics to CSV files.\n    process_measurements(\n        datafolder = simulation_info.datafolder,\n        n_bins = N_sim_bins,\n        export_to_csv = true,\n        scientific_notation = false,\n        decimals = 7,\n        delimiter = \" \"\n    )\n\n    return nothing\nend # end of `run_simulation` function\n\n\n# Only execute if the script is run directly from the command line.\nif abspath(PROGRAM_FILE) == @__FILE__\n\n    # Run the simulation, reading in command line arguments.\n    run_simulation(;\n        sID             = parse(Int,     ARGS[1]),\n        L               = parse(Int,     ARGS[2]),\n        U               = parse(Float64, ARGS[3]),\n        density         = parse(Float64, ARGS[4]),\n        ph_transform    = parse(Bool,    ARGS[5]),\n        N_equil         = parse(Int,     ARGS[6]),\n        N_opt           = parse(Int,     ARGS[7]),\n        N_sim           = parse(Int,     ARGS[8]),\n        N_opt_bins      = parse(Int,     ARGS[9]),\n        N_sim_bins      = parse(Int,     ARGS[10])\n    )\nend","title":"Hubbard Square"},{"category":"section","location":"simulation_output.html#Simulation-Output-Overview","page":"Simulation Output Overview","text":"","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"In this section we define and provide an overview of the output generated by a VMC simulation performed using the VariationalMC.jl package.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Output-Data-Folder-Structure","page":"Simulation Output Overview","text":"","title":"Output Data Folder Structure"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"Each simulation performed with VariationalMC.jl will generate a directory with a user defined name that all the output of the simulation will be written to. Here we will simply refer to this top-level directory as the datafolder, but again, the user may call it whatever they like. Inside the datafolder a set of files and folders resembling the structure outlined below will be found; note the CSV files will only be present if results are exported to CSV files as the default storage format is HDF5 files:","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"[datafolder]: \nmodel_summary.toml: A TOML file that describes the Hamiltonian that was simulated, and defines the various types of IDs that measurements are reported in terms of.\nsimulation_info_pID*_sID*.toml: A TOML file reporting basic information about the simulation, including the version of Julia and VariationalMC.jl that was used to run the simulation. An [metadata] table contains the contents of a user defined dictionary as well. One such file is generated for each pID MPI rank if multiple simulations are being performed in parallel using MPI.\nstats.h5: HDF5 file containing computed measurement statistics.\n[opt_bins]: Directory containing binned optimization measurement data stored in HDF5 files.\nbins_pID-0.h5: HDF5 file containing the binned measurement data associated with pID = 0.\nbins_pID-1.h5: HDF5 file containing the binned measurement data associated with pID = 1.\n[...]\n[sim_bins]: Directory containing binned simulation measurement data stored in HDF5 files.\nbins_pID-0.h5: HDF5 file containing the binned measurement data associated with pID = 0.\nbins_pID-1.h5: HDF5 file containing the binned measurement data associated with pID = 1.\n[...]\nglobal_stats.csv: CSV file containing final average and error for all global measurements.\nlocal_stats.csv: CSV file containing final average and error for all local measurements.\n[equal-time]: Top-level directory containing all equal-time correlation function measurement results.\n[density]: As an example, directory containing equal-time density-density correlations.\ndensity_momentum_equal-time_stats.csv: CSV file containing average and error for density-density correlation in momentum space.\ndensity_position_equal-time_stats.csv: CSV file containing average and error for density-density correlation in position space.\n[...]","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Model-Summary","page":"Simulation Output Overview","text":"","title":"Model Summary"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"The function model_summary is used to a TOML file named model_summary.toml that full describes and parameterizes the initial Hamiltonian simulated in a VMC simulation using VariationalMC.jl. Significantly, this method also defines the types of IDs (ORBITAL_ID, BOND_ID, HOPPING_ID) that the various measurements are reported in terms of. The type of ID used to report correlation and local measurements are recorded in the exported dictionaries CORRELATION_FUNCTIONS and LOCAL_MEASUREMENTS.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Uncertainty-Reporting-Conventions","page":"Simulation Output Overview","text":"","title":"Uncertainty Reporting Conventions"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"The average value barC for any measured expectation value langle C rangle is reported as a complex number in all  *_stats.csv files, even if it is known formally to be a strictly real quantity. On the other hand, the error, or standard deviation, associated with each measurement is reported as a strictly positive real number such that the error associated with the complex expectation value langle C rangle is given by Delta C = sqrtDelta C_rm Re^2 + Delta C_rm Im^2.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Global-Measurements","page":"Simulation Output Overview","text":"","title":"Global Measurements"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"At the end of a simulation once the measurements are processed using the process_measurements method, a file global_stats.csv file is created that contains the final estimates and errors for all \"global\" measurements. The corresponding binned global data is stored as binary HDF5 files in either the opt_bins or sim_bins directories. We define each global measurement below.","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"density: Average global total charge density langle n rangle = langle n_uparrow rangle + langle n_downarrow rangle.\ndouble_occ: Average global double occupancy langle n_uparrow n_downarrow rangle.\nenergy_per_site: Average global energy per site langle ErangleN.\nspin-z: Average global z-component of spin langle S^zrangle = langle n_uparrow - n_downarrowrangle2.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Local-Measurement","page":"Simulation Output Overview","text":"","title":"Local Measurement"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"At the end of a simulation once the measurements are processed using the process_measurements method, a file local_stats.csv file is created that contains the final estimates and errors for all \"local\" measurements. The corresponding binned local data is stored as binary HDF5 files in either the opt_bins or sim_bins directories. All \"local\" measurements are reported in terms of an ID type, defined in the model_summary.toml file generated by the model_summary function. The definition for each local measurement that may be reported, including the type of ID it is reported in terms of, is defined below.","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"density: (ORBITAL_ID) The charge density langle n_nu rangle = langle n_uparrownu rangle + langle n_downarrownu rangle for orbital nu in the unit cell.\ndouble_occ: (ORBITAL_ID) The double-occupance langle n_uparrownu n_downarrownu rangle for orbital nu in the unit cell.\nspin-z: (ORBITAL_ID) The z-component of spin langle S^z_nurangle = langle n_uparrow nu - n_downarrownurangle2 for orbital nu in the unit cell.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Local-Tight-Binding-Measurements","page":"Simulation Output Overview","text":"","title":"Local Tight-Binding Measurements"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"hopping_energy: (HOPPING_ID) The total hopping energy -sum_sigma langle t_lsigma(mathbfi+mathbfrnu)(mathbfigamma) c^dagger_sigmamathbfi+mathbfrnu c^phantomdagger_sigmamathbfigamma + rm hc rangle for a hopping connecting orbitals gamma and nu displaced mathbfr unit cells, where t_l(mathbfi+mathbfrnu)(mathbfigamma) is the hopping amplitude.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Local-Hubbard-Measurements","page":"Simulation Output Overview","text":"","title":"Local Hubbard Measurements"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"hubbard_energy: (ORBITAL_ID) The Hubbard interaction energy U_nu langle n_uparrownu n_downarrownu rangle for orbital nu in the unit cell.\nextended_hubbard_energy: (ORBITAL_ID) The extended Hubbard interaction energy V_nu langle n_mathbfigamma n_mathbfi+mathbfrnu rangle connecting orbitals gamma and nu displaced mathbfr unit cells.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Correlation-Function-Measurements","page":"Simulation Output Overview","text":"","title":"Correlation Function Measurements"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"Let hatO_mathbfinu represent some operator associated with unit cell mathbfi and ID index nu. For instance, in the case of a density-density correlation, nu corresponds to an ORBITAL_ID index. The only type of correlation that may be reported is the equal-time correlation function of the form","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"C_nugamma(mathbfr) = frac1N sum_mathbfi langle O^dagger_mathbfi+mathbfrnu O^phantomdagger_mathbfigamma rangle","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"in position space, where the sum over mathbfi runs over all unit cells, and mathbfr represents a displacement in unit cells. The corresponding equal-time correlation function in momentum space is given by","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"C_nugamma(mathbfq) = sum_mathbfr e^-rm imathbfqcdot(mathbfr+mathbfr_nu-mathbfr_gamma) C_nugamma(mathbfr)","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"where mathbfr_nu and mathbfr_gamma are static displacement vector associated with the operators hatO_mathbfinu and hatO_mathbfigamma. For instance, in the case of the density-density correlation, mathbfr_nu is the location of the orbital nu in the unit cell.","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Displacement-Vector-and-Momentum-Reporting-Conventions","page":"Simulation Output Overview","text":"","title":"Displacement Vector and Momentum Reporting Conventions"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"Consider a D dimensional finite periodic lattice, where mathbfa_d and mathbfb_d denote the corresponding lattice vectors and reciprocal lattice vectors, where d in 1D. The extent of the lattice in unit cells in the direction of each of the lattice vectors is denoted by L_d. Given these definitions, the displacement vectors are parametetized as","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"mathbfr = sum_d=1^D mathttR_d mathbfa_d","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"and then reported in terms of the set of integers mathttR_d in 0 L_d). Similarly, the set of mathbfk-points associated with the finite lattice are given by","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"mathbfk = sum_d=1^D left( fracmathttK_dL_d right) mathbfb_d","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"and then reported in term of the set of integers mathttK_d in 0 L_d).","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Correlation-Function-Reporting-Conventions","page":"Simulation Output Overview","text":"","title":"Correlation Function Reporting Conventions"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"In addition to reporting the set of integers mathttR_d  (mathttK_d) used to define correlation functions in position (momentum) space, they are also identified by an ID pair (mathtt*_ID_2 mathtt*_ID_1) specifying the subscripts nu and gamma. ","title":"Simulation Output Overview"},{"category":"section","location":"simulation_output.html#Defined-Correlation-Functions","page":"Simulation Output Overview","text":"","title":"Defined Correlation Functions"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"In this section we review the various correlation measurements that the VariationalMC.jl package currently supports.","title":"Simulation Output Overview"},{"category":"page","location":"simulation_output.html","page":"Simulation Output Overview","text":"density: (ORBITAL_ID) Measures total density correlation function.\nspin_z: (ORBITAL_ID) Measures spin-z correlation function.","title":"Simulation Output Overview"},{"category":"page","location":"index.html","page":"Home","text":"(Image: VariationalMC logo)","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"Documentation for VariationalMC.jl. This package implements the Variational Monte Carlo (VMC) method for the Hubbard model and models of electron-phonon interaction (coming in version 1.1.0). ","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"This code is currently the experimental stage of development. Use with caution.","title":"Home"},{"category":"section","location":"index.html#Funding","page":"Home","text":"","title":"Funding"},{"category":"page","location":"index.html","page":"Home","text":"The development of this code was supported by the U.S. Department of Energy, Office of Science, Office of Workforce Development for Teachers and Scientists, Office of Science Graduate Student Research (SCGSR) program. The SCGSR program is administered by the Oak Ridge Institute for Science and Education (ORISE) for the DOE. ORISE is managed by ORAU under contract number DE-SC0014664.","title":"Home"},{"category":"section","location":"index.html#Installation","page":"Home","text":"","title":"Installation"},{"category":"page","location":"index.html","page":"Home","text":"To install VariationalMC.jl, simply open the Julia REPL and run the commands","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"julia> ]\npkg> add https://github.com/atanjaro/VariationalMC.jl","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"or equivalently via Pkg do","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"julia> using Pkg; Pkg.add(url=\"https://github.com/atanjaro/VariationalMC.jl\")","title":"Home"},{"category":"section","location":"index.html#Documentation","page":"Home","text":"","title":"Documentation"},{"category":"page","location":"index.html","page":"Home","text":"DEV: Documentation associated with most recent commit to the main branch.","title":"Home"},{"category":"section","location":"index.html#Notable-Package-Dependencies","page":"Home","text":"","title":"Notable Package Dependencies"},{"category":"page","location":"index.html","page":"Home","text":"This section reviews some notable package dependencies.","title":"Home"},{"category":"section","location":"index.html#Re-exported-Packages","page":"Home","text":"","title":"Re-exported Packages"},{"category":"page","location":"index.html","page":"Home","text":"VariationalMC.jl re-exports certain packages from SmoQyDQMC.jl using the Reexport.jl package in order to simplify the installation process.","title":"Home"},{"category":"page","location":"index.html","page":"Home","text":"LatticeUtilties.jl: Used to represent arbitrary lattice geometries.","title":"Home"},{"category":"section","location":"index.html#External-Dependencies","page":"Home","text":"","title":"External Dependencies"},{"category":"page","location":"index.html","page":"Home","text":"HDF5.jl: Used for handling binary files.\nJLD2.jl: Used to write data to binary files in an HDF5 compatible format.","title":"Home"},{"category":"section","location":"index.html#Contact-Us","page":"Home","text":"","title":"Contact Us"},{"category":"page","location":"index.html","page":"Home","text":"For questions and comments regarding this package, please email Andy Tanjaroon Ly at atanjaro@vols.utk.edu or atanjaly82@gmail.com.","title":"Home"}]
}
